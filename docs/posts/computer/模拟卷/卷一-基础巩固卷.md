---
title: "卷一·基础巩固卷"
description: "广东专升本计算机 · 模拟卷（一）基础巩固"
category: "computer-note"
part: "模拟试卷"
---

# 广东专升本《计算机基础与程序设计》模拟卷（一）

> **基础巩固卷** · 难度：基础 60% + 中档 30% + 拔高 10%  
> **考试时间**：150 分钟　**满分**：200 分  
> **题型**：单选 20 题 + 判断 10 题 + 填空 5 题 + 简答 4 题 + 计算 3 题 + 应用 3 题

---

## 一、单项选择题（每小题 3 分，共 60 分）

**1.** 以下选项中，合法的 C 语言标识符是（　）

A. `2ab`　　B. `int`　　C. `_name`　　D. `a-b`

**2.** 设 `int a = 5, b = 2;`，则表达式 `a / b` 的值是（　）

A. 2.5　　B. 2　　C. 3　　D. 2.0

**3.** 若有定义 `int x = 3, y = 4, z = 5;`，则表达式 `(x > y) && (y < z)` 的值为（　）

A. 0　　B. 1　　C. 3　　D. 5

**4.** 以下语句中，能正确从键盘输入一个整数到变量 `n` 的是（　）

A. `scanf("%d", n);`　　B. `scanf("%d", &n);`　　C. `scanf("%d", &n);`　　D. `scanf("%d", n);`

**5.** 在 C 语言中，`switch` 语句中漏写 `break` 会导致（　）

A. 编译错误　　B. 程序崩溃　　C. 继续执行下一个 case（穿透）　　D. 跳出 switch

**6.** 若有定义 `int a[5] = {1, 2, 3};`，则 `a[3]` 的值是（　）

A. 0　　B. 3　　C. 随机值　　D. 编译错误

**7.** 关于字符串函数 `strlen("hello")` 的返回值，正确的是（　）

A. 6　　B. 5　　C. 不确定　　D. 取决于编译器

**8.** 以下函数定义正确的是（　）

A. `int f() { return 1; }`　　B. `void f() { return 1; }`  
C. `int f {} { return 1; }`　　D. `f() { return 1; }`

**9.** 若有定义 `int *p, a = 10; p = &a;`，则 `*p` 的值是（　）

A. a 的地址　　B. 10　　C. p 的地址　　D. 不确定

**10.** 以下关于结构体定义的说法，正确的是（　）

A. 结构体是 C 语言系统提供的类型　　B. 结构体是用户自定义的数据类型  
C. 结构体成员不能是数组　　D. 结构体变量不能作为函数参数

**11.** 函数 `fopen` 打开文件失败时返回（　）

A. 0　　B. EOF　　C. NULL　　D. -1

**12.** 带头结点的单链表为空的条件是（　）

A. `head == NULL`　　B. `head->next == NULL`　　C. `head->next == head`　　D. `head == 0`

**13.** 在单链表中删除 p 所指结点的直接后继，正确的操作是（　）

A. `p->next = p;`　　B. `p = p->next;`  
C. `p->next = p->next->next;`　　D. `p->next->next = p->next;`

**14.** 栈的进出原则是（　）

A. 先进先出　　B. 先进后出　　C. 随机存取　　D. 顺序存取

**15.** 深度为 4 的二叉树最多有多少个结点？（　）

A. 7　　B. 8　　C. 15　　D. 16

**16.** 二叉树的**先序遍历**顺序是（　）

A. 根→左→右　　B. 左→根→右　　C. 左→右→根　　D. 根→右→左

**17.** 完全二叉树有 12 个结点，其叶子结点数为（　）

A. 5　　B. 6　　C. 7　　D. 8

**18.** 图的深度优先遍历（DFS）使用的辅助数据结构是（　）

A. 队列　　B. 栈　　C. 数组　　D. 链表

**19.** 二分查找要求数据（　）

A. 无序存储　　B. 有序存储　　C. 链表存储　　D. 任意存储

**20.** 冒泡排序在最坏情况下的时间复杂度是（　）

A. O(n)　　B. O(n log n)　　C. O(n²)　　D. O(log n)

---

## 二、判断题（每小题 2 分，共 20 分。正确打 √，错误打 ×）

**21.** C 语言中，数组下标从 1 开始。（　）

**22.** `if (a = 5)` 在 C 语言中总是为真。（　）

**23.** `do-while` 循环至少执行一次循环体。（　）

**24.** `sizeof("abc")` 的值为 3。（　）

**25.** 在函数内部定义的静态局部变量在程序结束时才释放。（　）

**26.** 指针变量可以存储任意整数值。（　）

**27.** 以 `"r"` 方式打开一个不存在的文件会返回 NULL。（　）

**28.** 循环链表中判断是否到达表尾的条件是 `p->next == NULL`。（　）

**29.** 栈和队列的共同点是只允许在端点处进行插入和删除操作。（　）

**30.** 一棵二叉树中，叶子结点数可能多于总结点数的一半。（　）

---

## 三、填空题（每小题 4 分，共 20 分）

**31.** 在 C 语言中，`break` 语句的作用是跳出当前______。

**32.** 若有 `int a[3][4] = { {1}, {0, 2}, {3} };`，则 `a[1][2]` 的值为______。

**33.** 结构体成员访问运算符有两种：`.` 和 ______。

**34.** 栈的英文缩写是 ______，队列的英文缩写是 ______。

**35.** 在完全二叉树中，若结点总数为 n，则叶子结点数为 ______（向上取整）。

---

## 四、简答题（每小题 10 分，共 40 分）

**36.** 简述 C 语言中 `#include <stdio.h>` 的作用。

**37.** 简述传值调用和传址调用的区别。

**38.** 简述顺序表和链表的主要区别（至少 3 点）。

**39.** 简述二叉树先序遍历、中序遍历、后序遍历的定义。

---

## 五、计算题（每小题 10 分，共 30 分）

**40.** 写出以下程序的运行结果：

```c
#include <stdio.h>
int main() {
    int a = 5, b = 3, c;
    c = (a > b) ? a : b;
    printf("%d\n", c);
    return 0;
}
```

**41.** 写出以下程序的运行结果：

```c
#include <stdio.h>
int main() {
    int arr[5] = {10, 20, 30, 40, 50};
    int i, sum = 0;
    for (i = 0; i < 5; i++) {
        if (i % 2 == 0) sum += arr[i];
    }
    printf("%d\n", sum);
    return 0;
}
```

**42.** 给定二叉树的中序遍历序列为 `DBEAFC`，前序遍历序列为 `ABDECF`，请画出该二叉树。

---

## 六、应用题（每小题 10 分，共 30 分）

**43.** 编写程序，从键盘输入 10 个整数，求它们的和及平均值，并输出结果。

**44.** 编写程序，输入一个字符串（长度不超过 100），统计其中大写字母、小写字母和数字字符的个数。

**45.** 编写程序，定义一个包含 10 个整数的数组，将其按从小到大的顺序排序（使用冒泡排序法），并输出排序后的结果。