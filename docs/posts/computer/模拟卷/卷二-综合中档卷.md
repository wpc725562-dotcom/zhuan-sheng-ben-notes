---
title: "卷二·综合中档卷"
description: "广东专升本计算机 · 模拟卷（二）综合中档"
---

# 广东专升本《计算机基础与程序设计》模拟卷（二）

> **综合中档卷** · 难度：基础 30% + 中档 50% + 拔高 20%  
> **考试时间**：150 分钟　**满分**：200 分  
> **题型**：单选 20 题 + 判断 10 题 + 填空 5 题 + 简答 4 题 + 计算 3 题 + 应用 3 题

---

## 一、单项选择题（每小题 3 分，共 60 分）

**1.** 以下不能作为 C 语言常量的是（　）

A. `0x1F`　　B. `'a'`　　C. `"abc"`　　D. `1.2E`

**2.** 设 `int a = 6, b = 4;`，则表达式 `!(a > b) && (a != b)` 的值为（　）

A. 0　　B. 1　　C. 6　　D. 4

**3.** 设 `int x = 10;`，执行 `x += x -= x * x;` 后，x 的值为（　）

A. 0　　B. -180　　C. -90　　D. 100

**4.** 以下关于 `scanf` 函数的说法，错误的是（　）

A. 使用 `scanf` 读取字符串时，数组名前不需要加 `&`  
B. `scanf` 可以读取带空格的字符串  
C. `scanf("%d", &n)` 正确读取一个整数  
D. `scanf` 的格式控制串中可以包含普通字符

**5.** 若有定义 `char s[] = "hello";`，则 `sizeof(s)` 的值是（　）

A. 5　　B. 6　　C. 7　　D. 不确定

**6.** 若有定义 `int a[3][4]`，则以下对数组元素的引用正确的是（　）

A. `a[3][4]`　　B. `a[1,2]`　　C. `a[1][2]`　　D. `a(1)(2)`

**7.** 以下关于函数递归的说法，正确的是（　）

A. 递归一定比循环效率高　　B. 递归函数必须有一个终止条件  
C. 所有问题都适合递归解决　　D. 递归函数不能有参数

**8.** 以下关于静态局部变量的说法，正确的是（　）

A. 函数调用结束时被释放　　B. 只初始化一次，值在函数调用之间保持  
C. 只能在定义它的函数外部访问　　D. 默认为随机值

**9.** 若有 `int a = 10, *p = &a, **pp = &p;`，则 `**pp` 的值是（　）

A. a 的地址　　B. p 的地址　　C. 10　　D. pp 的地址

**10.** 以下关于 `const` 修饰指针的说法，正确的是（　）

A. `const int *p;` 表示指针 p 本身不可修改  
B. `int *const p;` 表示 p 指向的值不可修改  
C. `const int *p;` 表示 p 指向的值不可通过 p 修改  
D. `const` 不能修饰指针

**11.** 关于 `typedef` 的用法，以下正确的是（　）

A. `typedef int INT;` 定义了一个新类型 INT  
B. `typedef` 可以创建全新的数据类型  
C. `typedef` 只能在函数内部使用  
D. `typedef` 和 `#define` 完全等价

**12.** 函数 `fseek(fp, 0L, SEEK_END);` 的作用是（　）

A. 将文件指针移到文件开头　　B. 将文件指针移到文件末尾  
C. 将文件指针向前移动 0 个字节　　D. 判断文件是否结束

**13.** 在双向链表中，在 p 所指结点之前插入 s 结点的操作，正确的是（　）

A. `s->next = p; p->prior = s;`  
B. `s->next = p; s->prior = p->prior; p->prior->next = s; p->prior = s;`  
C. `p->prior = s; s->next = p;`  
D. `s->next = p->prior; p->prior = s;`

**14.** 循环队列中，判断队满的条件是（假设队列大小为 MAXSIZE）（　）

A. `front == rear`　　B. `front == rear + 1`  
C. `(rear + 1) % MAXSIZE == front`　　D. `rear == MAXSIZE`

**15.** 若已知一棵二叉树的中序序列为 `BDCA`，后序序列为 `DCBA`，则该二叉树的根结点是（　）

A. B　　B. C　　C. A　　D. D

**16.** 在一棵二叉排序树（BST）中，中序遍历得到的序列是（　）

A. 递减有序　　B. 递增有序　　C. 无序　　D. 根结点在中间

**17.** 具有 6 个顶点的无向连通图，最少需要多少条边？（　）

A. 4　　B. 5　　C. 6　　D. 7

**18.** 图的广度优先遍历（BFS）使用的辅助数据结构是（　）

A. 栈　　B. 队列　　C. 数组　　D. 链表

**19.** 折半查找（二分查找）要求数据（　）

A. 以顺序方式存储且已排序　　B. 以链式方式存储且已排序  
C. 以顺序方式存储，无需排序　　D. 以链式方式存储，无需排序

**20.** 下列排序算法中，**不稳定**的是（　）

A. 冒泡排序　　B. 直接插入排序　　C. 快速排序　　D. 归并排序

---

## 二、判断题（每小题 2 分，共 20 分。正确打 √，错误打 ×）

**21.** C 语言中，`main` 函数是程序的入口，每个程序只能有一个 `main` 函数。（　）

**22.** 在 C 语言中，`'a'` 和 `"a"` 是等价的。（　）

**23.** 函数可以返回数组类型。（　）

**24.** 数组名作为函数参数时，传递的是数组的首地址。（　）

**25.** 指针变量占用内存空间，其大小与指针指向的数据类型有关。（　）

**26.** 结构体类型的变量可以作为函数参数传递。（　）

**27.** 以 `"w"` 方式打开一个已存在的文件，会清空文件原有内容。（　）

**28.** 带头结点的单链表，头结点中不存储数据，只存储指向第一个结点的指针。（　）

**29.** 栈和队列都是线性结构，且都只能在端点进行插入和删除操作。（　）

**30.** 图的深度优先遍历序列一定是唯一的。（　）

---

## 三、填空题（每小题 4 分，共 20 分）

**31.** 若有定义 `int a[5] = {1, 2, 3, 4, 5};`，则 `a[5]` 是______（合法/非法）访问。

**32.** 函数 `void f(int a[], int n)` 中，形参 `a` 本质上是一个______。

**33.** 若有 `int *p = NULL;`，则此时 `*p` 是______操作。

**34.** 在单链表中插入一个新结点，需要修改______个指针域。

**35.** 对一棵二叉排序树进行______遍历，可以得到递增有序序列。

---

## 四、简答题（每小题 10 分，共 40 分）

**36.** 简述递归函数设计的基本要素（至少 3 点）。

**37.** 简述结构体和共用体（union）的主要区别。

**38.** 简述顺序表插入元素的基本步骤。

**39.** 简述图的深度优先遍历（DFS）和广度优先遍历（BFS）的区别。

---

## 五、计算题（每小题 10 分，共 30 分）

**40.** 写出以下程序的运行结果：

```c
#include <stdio.h>
int f(int n) {
    if (n <= 1) return 1;
    return n * f(n - 1);
}
int main() {
    printf("%d\n", f(4));
    return 0;
}
```

**41.** 写出以下程序的运行结果：

```c
#include <stdio.h>
#define N 5
int main() {
    int queue[N] = {0};
    int front = 0, rear = 0;
    // 入队
    queue[rear] = 10; rear = (rear + 1) % N;
    queue[rear] = 20; rear = (rear + 1) % N;
    queue[rear] = 30; rear = (rear + 1) % N;
    // 出队
    front = (front + 1) % N;
    // 入队
    queue[rear] = 40; rear = (rear + 1) % N;
    queue[rear] = 50; rear = (rear + 1) % N;
    printf("front=%d, rear=%d\n", front, rear);
    return 0;
}
```

**42.** 给定无向图 G 的邻接矩阵如下（顶点编号 1~5）：

```
    1 2 3 4 5
  1 0 1 1 0 0
  2 1 0 0 1 1
  3 1 0 0 0 1
  4 0 1 0 0 1
  5 0 1 1 1 0
```

请画出该图，并写出从顶点 1 出发的深度优先遍历序列（按编号升序访问邻接点）。

---

## 六、应用题（每小题 10 分，共 30 分）

**43.** 编写程序，输入 n（n ≤ 20）个整数，输出其中的最大值和最小值。

**44.** 编写程序，实现字符串复制功能（不使用 `strcpy` 函数）。要求从键盘输入一个字符串，将其复制到另一个字符数组中，并输出复制后的字符串。

**45.** 编写程序，定义一个学生结构体 `struct Student`，包含学号（int）、姓名（char[20]）和成绩（float）。从键盘输入 3 个学生的信息，输出成绩最高的学生信息。