---
title: "卷二·综合中档卷（答案）"
description: "广东专升本计算机 · 模拟卷（二）参考答案"
---

# 模拟卷（二）· 综合中档卷 · 参考答案

---

## 一、单项选择题（20 题 × 3 分 = 60 分）

| 题号 | 答案 | 考点 | 解析 |
|:---:|:---:|:---|---|
| 1 | **D** | 1.2 常量 | `1.2E` 不是合法的浮点数常量（指数部分缺少指数值）。 |
| 2 | **A** | 1.2 逻辑运算 | `!(a > b)` = `!(6 > 4)` = `!1` = 0，逻辑与 `&&` 短路，结果为 0。 |
| 3 | **B** | 1.2 复合赋值 | 从右向左：`x -= x * x` => `x = 10 - 100 = -90`；`x += -90` => `x = -90 + (-90) = -180`。 |
| 4 | **B** | 1.3 scanf | `scanf` 遇到空格会停止读取，不能读取带空格的字符串。应使用 `gets` 或 `fgets`。 |
| 5 | **B** | 1.6 sizeof | `sizeof(s)` 包含结尾的 `\0`，`"hello"` 占 6 字节。 |
| 6 | **C** | 1.6 数组引用 | 二维数组引用格式为 `a[i][j]`，A 下标越界，B 语法错误，D 语法错误。 |
| 7 | **B** | 1.7 递归 | 递归必须有终止条件（递归出口），否则会无限递归导致栈溢出。 |
| 8 | **B** | 1.7 静态变量 | 静态局部变量在程序运行期间只初始化一次，值在函数调用间保持。 |
| 9 | **C** | 1.8 二级指针 | `pp` 指向 `p`，`p` 指向 `a`，`**pp` = `*p` = `a` = 10。 |
| 10 | **C** | 1.8 const 指针 | `const int *p`：p 指向的值不可通过 p 修改（指向常量）。 |
| 11 | **A** | 1.9 typedef | `typedef int INT;` 给 `int` 起别名 `INT`，不创建新类型。 |
| 12 | **B** | 1.10 文件操作 | `fseek(fp, 0L, SEEK_END)` 将文件指针定位到文件末尾。 |
| 13 | **B** | 2.2 双向链表 | 双向链表插入：先连接 s 的前驱后继指针，再修改 p 前驱的 next 和 p 的 prior。 |
| 14 | **C** | 2.3 循环队列 | 循环队列判满：`(rear + 1) % MAXSIZE == front`（牺牲一个单元）。 |
| 15 | **C** | 2.5 遍历还原 | 后序 `DCBA` 最后一个元素 A 为根结点。 |
| 16 | **B** | 2.5 BST | 二叉排序树中序遍历得到递增有序序列。 |
| 17 | **B** | 2.6 图 | n 个顶点的无向连通图最少需要 n-1 条边（树形结构），6-1=5。 |
| 18 | **B** | 2.6 图遍历 | BFS 借助队列实现层次遍历，DFS 借助栈。 |
| 19 | **A** | 2.7 查找 | 折半查找要求顺序存储且有序。 |
| 20 | **C** | 2.8 排序 | 快速排序不稳定（口诀：快些选一堆）。 |

---

## 二、判断题（10 题 × 2 分 = 20 分）

| 题号 | 答案 | 考点 | 解析 |
|:---:|:---:|:---|---|
| 21 | **√** | 1.1 main | main 是程序入口，一个 C 程序只能有一个 main 函数。 |
| 22 | **×** | 1.2 字符 | `'a'` 是字符常量，`"a"` 是字符串常量（含 `\0`），不等价。 |
| 23 | **×** | 1.7 函数 | 函数不能返回数组类型，但可以返回指针。 |
| 24 | **√** | 1.7 数组传参 | 数组名作为参数传递的是首地址（传址调用）。 |
| 25 | **×** | 1.8 指针 | 指针大小与指向类型无关，32 位系统 4 字节，64 位系统 8 字节。 |
| 26 | **√** | 1.9 结构体 | 结构体变量可以作为函数参数（传值或传址）。 |
| 27 | **√** | 1.10 文件操作 | `"w"` 方式打开已存在文件会清空原内容。 |
| 28 | **√** | 2.2 线性表 | 带头结点链表的头结点通常不存储数据。 |
| 29 | **√** | 2.3 栈和队列 | 都是受限线性表，只能在端点操作。 |
| 30 | **×** | 2.6 图遍历 | 遍历序列不唯一，取决于访问邻接点的顺序。 |

---

## 三、填空题（5 题 × 4 分 = 20 分）

| 题号 | 答案 | 考点 |
|:---:|:---|---|
| 31 | **非法** 或 **越界** | 1.6 数组下标 |
| 32 | **指针** | 1.7 数组传参退化 |
| 33 | **非法** 或 **空指针访问** | 1.8 空指针 |
| 34 | **2** | 2.2 单链表插入 |
| 35 | **中序** | 2.5 BST 性质 |

---

## 四、简答题（4 题 × 10 分 = 40 分）

### 36. 递归函数设计的基本要素（10 分，至少 3 点）

**评分要点**：
1. **递归出口**（终止条件）：当满足某个条件时，不再递归调用（4 分）
2. **递归公式**：将原问题分解为规模更小的子问题（3 分）
3. **参数变化**：每次递归调用时参数向终止条件靠近（3 分）

### 37. 结构体和共用体的区别（10 分）

**评分要点**：

| 对比项 | 结构体（struct） | 共用体（union） |
|:---|:---|:---|
| 内存分配 | 各成员独立分配，总大小为各成员之和（考虑对齐）（3 分） | 所有成员共享同一块内存，大小为最大成员的大小（3 分） |
| 成员访问 | 各成员可同时使用（2 分） | 同一时刻只能使用一个成员（2 分） |

### 38. 顺序表插入元素的基本步骤（10 分）

**评分要点**：
1. 检查插入位置是否合法（1 分）
2. 检查顺序表是否已满（2 分）
3. 从最后一个元素开始到插入位置，依次向后移动一个位置（4 分）
4. 将要插入的元素放入空出的位置（2 分）
5. 表长加 1（1 分）

### 39. DFS 和 BFS 的区别（10 分）

**评分要点**：

| 对比项 | DFS | BFS |
|:---|:---|:---|
| 辅助结构 | 栈（3 分） | 队列（3 分） |
| 遍历策略 | 尽可能深地搜索，回溯（2 分） | 按层逐层遍历（2 分） |

---

## 五、计算题（3 题 × 10 分 = 30 分）

### 40. 递归函数运行结果（10 分）

**结果**：**24**（10 分）

**解析**：`f(4)` 递归展开：
- `f(4) = 4 × f(3)`
- `f(3) = 3 × f(2)`
- `f(2) = 2 × f(1)`
- `f(1) = 1`
- 回代：`f(2) = 2`，`f(3) = 6`，`f(4) = 24`

### 41. 循环队列运行结果（10 分）

**结果**：**front=1, rear=0**（10 分）

**解析**：
- 初始：front=0, rear=0，队列空
- 入队 10：rear=1
- 入队 20：rear=2
- 入队 30：rear=3
- 出队一次：front=1（10 出队）
- 入队 40：rear=4
- 入队 50：rear=0（循环回绕）
- 最终：front=1, rear=0

### 42. 邻接矩阵 → 图 → DFS 序列（10 分）

**图结构**：
```
1 — 2
|   | \
3 — 5 — 4
```

**DFS 序列（从顶点 1，升序访问邻接点）**：**1 → 2 → 4 → 5 → 3**（10 分）

**步骤**：
1. 访问 1，邻接点 2、3（升序，先 2）
2. 访问 2，邻接点 1、4、5（1 已访问，先 4）
3. 访问 4，邻接点 2、5（2 已访问，访问 5）
4. 访问 5，邻接点 2、3、4（2、4 已访问，访问 3）
5. 访问 3，邻接点 1、5（全部已访问，回溯结束）

---

## 六、应用题（3 题 × 10 分 = 30 分）

### 43. 求最大最小值（10 分）

**评分标准**：
- 框架 2 分
- 输入 n 和数组 2 分
- 找最大最小值逻辑 4 分
- 输出 2 分

**参考答案**：

```c
#include <stdio.h>
int main() {
    int n, i, a[20], max, min;
    printf("请输入整数个数 n（≤20）：");
    scanf("%d", &n);
    printf("请输入 %d 个整数：", n);
    for (i = 0; i < n; i++) {
        scanf("%d", &a[i]);
    }
    max = min = a[0];
    for (i = 1; i < n; i++) {
        if (a[i] > max) max = a[i];
        if (a[i] < min) min = a[i];
    }
    printf("最大值 = %d，最小值 = %d\n", max, min);
    return 0;
}
```

### 44. 字符串复制（10 分）

**评分标准**：
- 框架 2 分
- 输入字符串 2 分
- 复制逻辑 4 分（逐字符复制 + 结尾加 `\0`）
- 输出 2 分

**参考答案**：

```c
#include <stdio.h>
int main() {
    char src[100], dest[100];
    int i = 0;
    printf("请输入一个字符串：");
    gets(src);
    while (src[i] != '\0') {
        dest[i] = src[i];
        i++;
    }
    dest[i] = '\0';
    printf("复制后的字符串：%s\n", dest);
    return 0;
}
```

### 45. 结构体编程（10 分）

**评分标准**：
- 结构体定义 3 分
- 输入 3 个学生信息 3 分
- 比较找最高分 2 分
- 输出 2 分

**参考答案**：

```c
#include <stdio.h>
struct Student {
    int id;
    char name[20];
    float score;
};
int main() {
    struct Student stu[3], max_stu;
    int i;
    for (i = 0; i < 3; i++) {
        printf("请输入第 %d 个学生的学号、姓名、成绩：", i + 1);
        scanf("%d %s %f", &stu[i].id, stu[i].name, &stu[i].score);
    }
    max_stu = stu[0];
    for (i = 1; i < 3; i++) {
        if (stu[i].score > max_stu.score) max_stu = stu[i];
    }
    printf("成绩最高的学生：学号=%d，姓名=%s，成绩=%.1f\n",
           max_stu.id, max_stu.name, max_stu.score);
    return 0;
}
```

---

> **卷二（综合中档卷）命题完成**  
> 下一卷：**卷三（拔高冲刺卷）**