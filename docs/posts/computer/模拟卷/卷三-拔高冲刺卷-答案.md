---
title: "卷三·拔高冲刺卷（答案）"
description: "广东专升本计算机 · 模拟卷（三）参考答案"
category: "computer-note"
part: "模拟试卷"
---

# 模拟卷（三）· 拔高冲刺卷 · 参考答案

---

## 一、单项选择题（20 题 × 3 分 = 60 分）

| 题号 | 答案 | 考点 | 解析 |
|:---:|:---:|:---|---|
| 1 | **C** | 1.1 注释 | 注释在编译时被忽略（预处理阶段删除），不参与编译。 |
| 2 | **B** | 1.2 自增运算 | `a++` 先使用 a 再自增（值为 5），`++b` 先自增再使用（值为 6），`5 + 6 = 11`。 |
| 3 | **B** | 1.2 优先级 | `+` 和 `-` 优先级高于 `||` 和 `&&`，等价于 `a || (b + c) && (b - c)` = `3 || 9 && -1` = `3 || 1` = `1`。 |
| 4 | **B** | 1.5 continue | `i` 为奇数时 `continue` 跳过本次循环，只有偶数 2、4 被输出。 |
| 5 | **A** | 1.6 strcmp | `strcmp("abc", "ABC")`，'a' 的 ASCII 码（97）大于 'A'（65），返回值大于 0。 |
| 6 | **D** | 1.6 二维数组 | 按行存储：`a[0] = {1,2,3}`，`a[1] = {4,5,6}`，`a[1][1] = 5`。 |
| 7 | **C** | 1.7 数组传参 | 数组名作为参数传递的是首地址（传址），而非整个数组的副本。 |
| 8 | **C** | 1.8 指针数组 | `int *p[5]` 是**指针数组**（p 是数组，元素是指针），`int (*p)[5]` 是**数组指针**（p 是指向数组的指针）。 |
| 9 | **D** | 1.8 指针运算 | `p` 指向 `a[0]`，`p + 3` 指向 `a[3]`，`*(p + 3) = 40`。 |
| 10 | **A** | 1.8 const 指针 | `const int *p`：p 指向的值不可通过 p 修改；`int *const p`：p 本身不可修改（常指针）。 |
| 11 | **A** | 1.9 union | union 所有成员共享内存，大小为最大成员大小，同一时刻只能使用一个成员。 |
| 12 | **B** | 1.10 fread | `fread` 返回成功读取的元素个数。 |
| 13 | **A** | 2.2 链表删除 | 已知前驱 q 时：`q->next = p->next; free(p);`。 |
| 14 | **D** | 2.3 出栈序列 | D 选项 4,1,5,2,3 不可能：4 先出栈说明 1,2,3 已在栈中，4 出栈后栈顶为 3，下一个出栈只能是 3 或 5，不能是 1。 |
| 15 | **D** | 2.5 哈夫曼树 | 构造：3+5=8，6+8=14，10+14=24。WPL = 3×3+5×3+6×2+10×2 = 9+15+12+20 = 56。 |
| 16 | **B** | 2.5 AVL | 平衡二叉树要求左右子树高度差不超过 1。 |
| 17 | **C** | 2.6 有向完全图 | n 个顶点的有向完全图有 n×(n-1) 条弧，5×4=20。 |
| 18 | **B** | 2.6 Prim | Prim 算法每次从当前已选顶点集合出发，选择权值最小的邻接顶点加入。 |
| 19 | **A** | 2.7 哈希 | 冲突指不同关键字映射到同一哈希地址。 |
| 20 | **C** | 2.8 排序 | 快排最坏情况（已有序）下 O(n²)。 |

---

## 二、判断题（10 题 × 2 分 = 20 分）

| 题号 | 答案 | 考点 | 解析 |
|:---:|:---:|:---|---|
| 21 | **√** | 1.1 main | C99 标准允许 main 省略 return 0，编译器会隐式返回 0。 |
| 22 | **×** | 1.2 转义字符 | `"\\"` 是转义后的反斜杠字符，长度为 1（含结尾 \0 占 2 字节）。 |
| 23 | **√** | 1.7 递归 | 递归调用每次在栈中分配新的空间，递归深度过大会导致栈溢出。 |
| 24 | **√** | 1.8 指针运算 | `p + i` 指向数组第 i 个元素，等价于 `&a[i]`。 |
| 25 | **×** | 1.10 文件操作 | `fclose` 失败会导致缓冲区数据未写入文件，可能丢失数据。 |
| 26 | **×** | 2.2 静态链表 | 静态链表用数组模拟链表，插入删除只需修改游标，不需要移动元素。 |
| 27 | **√** | 2.3 循环队列 | 循环队列判空条件为 `front == rear`。 |
| 28 | **×** | 2.5 二叉树 | 前序+后序无法唯一确定二叉树，必须含中序。 |
| 29 | **√** | 2.6 图 | 无向图的邻接矩阵是对称矩阵。 |
| 30 | **√** | 2.7 查找 | 折半查找的判定树是二叉排序树的一种。 |

---

## 三、填空题（5 题 × 4 分 = 20 分）

| 题号 | 答案 | 考点 |
|:---:|:---|---|
| 31 | **0** | 1.2 全局变量初始化 |
| 32 | **3** | 1.8 指针负下标 |
| 33 | **p->next** | 2.2 链表插入 |
| 34 | **O(n)** | 2.5 二叉树遍历 |
| 35 | **插入** | 2.8 希尔排序 |

---

## 四、简答题（4 题 × 10 分 = 40 分）

### 36. fprintf/fscanf 与 fwrite/fread 的区别（10 分）

**评分要点**：

| 对比项 | fprintf/fscanf | fwrite/fread |
|:---|:---|:---|
| 读写方式 | 格式化读写，按文本格式（5 分） | 二进制块读写，直接读写内存（5 分） |
| 可读性 | 生成的文件可读 | 生成的文件是二进制格式 |
| 效率 | 较低（需格式转换） | 较高（直接内存拷贝） |

### 37. 循环队列的设计目的和方法（10 分）

**评分要点**：
- **目的**：解决顺序队列的"假溢出"问题，充分利用数组空间（4 分）
- **判空**：`front == rear`（3 分）
- **判满**：`(rear + 1) % MAXSIZE == front`（牺牲一个单元）（3 分）

### 38. 哈夫曼树的构造过程和应用（10 分）

**评分要点**：
- **构造过程**：每次从集合中选出两个权值最小的结点作为左右子树，构造新结点（权值为两者之和），将新结点放回集合，重复直到只剩一个结点（6 分）
- **应用**：哈夫曼编码（数据压缩）、最优判定树（4 分）

### 39. 哈希冲突解决方法（10 分）

**评分要点**：
1. **开放地址法**：发生冲突时按某种探测序列（如线性探测、平方探测）寻找下一个空位（5 分）
2. **链地址法**：每个哈希地址对应一个链表，冲突元素挂在链表中（5 分）

---

## 五、计算题（3 题 × 10 分 = 30 分）

### 40. 指针传址运行结果（10 分）

**结果**：
```
x=10, y=5
*p=10, *q=5
```
（每行 5 分）

**解析**：`swap(p, q)` 交换了 p 和 q 指向的值（x 和 y），所以 x 和 y 的值互换。

### 41. 二叉树还原（10 分）

**步骤**：
1. 前序 `ABDEGCFH` → 根为 A（2 分）
2. 中序 `DBGEACHF` → A 左边为左子树（DBGE），右边为右子树（CHF）（2 分）
3. 左子树前序 `BDEG` → 根为 B，中序 `DBGE` → B 左边为 D，右边为 GE（3 分）
4. 右子树前序 `CFH` → 根为 C，中序 `CHF` → C 左边为空，右边为 HF（3 分）

```
      A
     / \
    B   C
   / \   \
  D   E   H
     /   /
    G   F
```

**更正后序序列**：`DGEBHFCA`

### 42. 最小生成树计算（10 分）

**Prim 算法（从顶点 1 开始）**：

| 步骤 | 已选顶点 | 可选边 | 选择边 | 说明 |
|:---:|:---|---|:---:|:---|
| 1 | {1} | (1,2,5), (1,3,3) | (1,3,3) | 最小权值 |
| 2 | {1,3} | (1,2,5), (3,2,4), (3,5,7) | (3,2,4) | 权值 4 最小 |
| 3 | {1,3,2} | (2,4,6), (2,5,2) | (2,5,2) | 权值 2 最小 |
| 4 | {1,3,2,5} | (2,4,6), (5,4,1), (5,6,8) | (5,4,1) | 权值 1 最小 |
| 5 | {1,3,2,5,4} | (4,6,9), (5,6,8) | (5,6,8) | 权值 8 最小 |
| 6 | {1,3,2,5,4,6} | — | 完成 | — |

**Prim 边序列**：`(1,3), (3,2), (2,5), (5,4), (5,6)`（5 分）

**Kruskal 算法**：

| 步骤 | 边 | 权值 | 操作 | 说明 |
|:---:|:---|---|:---:|:---|
| 1 | (4,5) | 1 | 选入 | 最小权值 |
| 2 | (2,5) | 2 | 选入 |  |
| 3 | (1,3) | 3 | 选入 |  |
| 4 | (2,3) | 4 | 选入 |  |
| 5 | (1,2) | 5 | 跳过 | 会形成环 |
| 6 | (5,6) | 8 | 选入 | 所有顶点连通 |

**Kruskal 边序列**：`(4,5), (2,5), (1,3), (2,3), (5,6)`（5 分）

---

## 六、应用题（3 题 × 10 分 = 30 分）

### 43. 递归求阶乘（10 分）

**评分标准**：
- 框架 2 分
- 递归函数定义 4 分（递归出口 + 递归公式各 2 分）
- 输入和调用 2 分
- 输出 2 分

**参考答案**：

```c
#include <stdio.h>
int fact(int n) {
    if (n <= 1) return 1;
    return n * fact(n - 1);
}
int main() {
    int n;
    printf("请输入 n（1~20）：");
    scanf("%d", &n);
    printf("%d! = %d\n", n, fact(n));
    return 0;
}
```

### 44. 结构体与平均成绩（10 分）

**评分标准**：
- 结构体定义 2 分
- 输入 3 个学生信息 3 分
- 计算平均成绩 2 分
- 比较找最高分 2 分
- 输出 1 分

**参考答案**：

```c
#include <stdio.h>
struct Student {
    int id;
    char name[20];
    float score[3];
};
int main() {
    struct Student stu[3], max_stu;
    float avg[3], max_avg;
    int i, j;
    for (i = 0; i < 3; i++) {
        printf("请输入第 %d 个学生的学号、姓名、三门课成绩：", i + 1);
        scanf("%d %s %f %f %f", &stu[i].id, stu[i].name,
              &stu[i].score[0], &stu[i].score[1], &stu[i].score[2]);
        avg[i] = (stu[i].score[0] + stu[i].score[1] + stu[i].score[2]) / 3.0;
    }
    max_avg = avg[0];
    max_stu = stu[0];
    for (i = 1; i < 3; i++) {
        if (avg[i] > max_avg) {
            max_avg = avg[i];
            max_stu = stu[i];
        }
    }
    printf("平均成绩最高的学生：学号=%d，姓名=%s，平均成绩=%.1f\n",
           max_stu.id, max_stu.name, max_avg);
    return 0;
}
```

### 45. 直接插入排序（10 分）

**评分标准**：
- 框架 2 分
- 数组定义和输入 2 分
- 直接插入排序核心逻辑 4 分
- 输出 2 分

**参考答案**：

```c
#include <stdio.h>
int main() {
    int a[10], i, j, key;
    printf("请输入 10 个整数：");
    for (i = 0; i < 10; i++) {
        scanf("%d", &a[i]);
    }
    // 直接插入排序
    for (i = 1; i < 10; i++) {
        key = a[i];
        j = i - 1;
        while (j >= 0 && a[j] > key) {
            a[j + 1] = a[j];
            j--;
        }
        a[j + 1] = key;
    }
    printf("排序结果：");
    for (i = 0; i < 10; i++) {
        printf("%d ", a[i]);
    }
    printf("\n");
    return 0;
}
```

---

> **卷三（拔高冲刺卷）命题完成**  
> **三套模拟卷全部命题完毕！**