---
title: "卷三·拔高冲刺卷"
description: "广东专升本计算机 · 模拟卷（三）拔高冲刺"
---

# 广东专升本《计算机基础与程序设计》模拟卷（三）

> **拔高冲刺卷** · 难度：基础 20% + 中档 40% + 拔高 40%  
> **考试时间**：150 分钟　**满分**：200 分  
> **题型**：单选 20 题 + 判断 10 题 + 填空 5 题 + 简答 4 题 + 计算 3 题 + 应用 3 题

---

## 一、单项选择题（每小题 3 分，共 60 分）

**1.** 以下关于 C 语言注释的说法，正确的是（　）

A. 注释可以嵌套　　B. 注释在编译时被处理  
C. 注释会被编译器忽略　　D. 注释只能写在一行内

**2.** 设 `int a = 5, b = 5;`，表达式 `a++ + ++b` 的值是（　）

A. 10　　B. 11　　C. 12　　D. 13

**3.** 若有定义 `int a = 3, b = 4, c = 5;`，则表达式 `a || b + c && b - c` 的值为（　）

A. 0　　B. 1　　C. 3　　D. 5

**4.** 以下程序段的输出结果是（　）

```c
int i;
for (i = 1; i <= 5; i++) {
    if (i % 2 == 1) continue;
    printf("%d ", i);
}
```

A. 1 3 5　　B. 2 4　　C. 1 2 3 4 5　　D. 无输出

**5.** 若有定义 `char s1[] = "abc", s2[] = "ABC";`，则 `strcmp(s1, s2)` 的返回值（　）

A. 大于 0　　B. 等于 0　　C. 小于 0　　D. 不确定

**6.** 若有定义 `int a[2][3] = {1, 2, 3, 4, 5, 6};`，则 `a[1][1]` 的值是（　）

A. 2　　B. 3　　C. 4　　D. 5

**7.** 以下关于函数参数传递的说法，正确的是（　）

A. 数组作为参数时，传递的是整个数组的副本  
B. 指针作为参数时，形参的改变不会影响实参  
C. 数组名作为参数时，传递的是数组首地址  
D. 结构体作为参数时，只能传址不能传值

**8.** 以下关于指针数组和数组指针的说法，正确的是（　）

A. `int *p[5];` 定义了一个指向数组的指针  
B. `int (*p)[5];` 定义了一个指针数组  
C. `int *p[5];` 中 p 是一个数组，每个元素是 int 指针  
D. 两者没有区别

**9.** 若有以下程序段，输出结果是（　）

```c
int a[] = {10, 20, 30, 40, 50};
int *p = a;
printf("%d\n", *(p + 3));
```

A. 10　　B. 20　　C. 30　　D. 40

**10.** 以下关于 `const int *p` 和 `int *const p` 的区别，正确的是（　）

A. 前者 p 指向的值不可变，后者 p 本身不可变  
B. 前者 p 本身不可变，后者 p 指向的值不可变  
C. 两者含义相同  
D. 两者都是 p 和指向的值都不可变

**11.** 关于 `union` 类型，以下说法正确的是（　）

A. 所有成员共享同一块内存，大小为最大成员的大小  
B. 所有成员各自独立分配内存  
C. 可以同时访问所有成员的值  
D. union 变量可以初始化多个成员

**12.** 函数 `fread` 的返回值表示（　）

A. 读取的字节数　　B. 成功读取的元素个数  
C. 文件指针位置　　D. 是否读到文件末尾

**13.** 在单链表中，删除 p 所指结点本身（而非后继）的正确操作是（前提：已知前驱结点 q）（　）

A. `q->next = p->next; free(p);`　　B. `p->next = q->next; free(p);`  
C. `free(p); q->next = p->next;`　　D. `q->next = p; free(p);`

**14.** 给定入栈序列 1, 2, 3, 4, 5，以下哪个**不可能**是出栈序列？（　）

A. 1, 2, 3, 4, 5　　B. 5, 4, 3, 2, 1　　C. 3, 2, 5, 4, 1　　D. 4, 1, 5, 2, 3

**15.** 一组权值 {3, 5, 6, 10} 构造的哈夫曼树的带权路径长度（WPL）是（　）

A. 44　　B. 48　　C. 52　　D. 56

**16.** 在一棵平衡二叉树（AVL）中，左右子树的高度差不超过（　）

A. 0　　B. 1　　C. 2　　D. 3

**17.** 具有 5 个顶点的有向完全图，共有多少条弧？（　）

A. 10　　B. 15　　C. 20　　D. 25

**18.** 使用 Prim 算法求最小生成树，每次选择的顶点是（　）

A. 权值最小的边对应的顶点　　B. 当前已选顶点集合中权值最小的邻接顶点  
C. 任意一个未选顶点　　D. 编号最小的顶点

**19.** 哈希查找中，冲突指的是（　）

A. 两个关键字有相同的哈希地址　　B. 两个关键字不相同  
C. 关键字被删除　　D. 哈希表已满

**20.** 快速排序在**最坏情况**下的时间复杂度是（　）

A. O(n)　　B. O(n log n)　　C. O(n²)　　D. O(log n)

---

## 二、判断题（每小题 2 分，共 20 分。正确打 √，错误打 ×）

**21.** 在 C 语言中，`main` 函数可以没有 `return 0;` 语句。（　）

**22.** 字符串 `"\\"` 的长度为 2。（　）

**23.** 函数的递归调用本质上是函数调用自身，每次调用都会在栈中分配新的空间。（　）

**24.** 指针变量 `p` 指向数组 `a`，则 `p + i` 与 `&a[i]` 等价。（　）

**25.** 文件操作中，`fclose` 函数失败时不会影响已写入的数据。（　）

**26.** 静态链表也使用数组存储，因此其插入和删除操作也需要移动大量元素。（　）

**27.** 循环队列中，队空的条件是 `front == rear`。（　）

**28.** 二叉树的前序序列和后序序列可以唯一确定一棵二叉树。（　）

**29.** 图的邻接矩阵表示法中，无向图的邻接矩阵是对称矩阵。（　）

**30.** 折半查找的判定树是一棵二叉排序树。（　）

---

## 三、填空题（每小题 4 分，共 20 分）

**31.** 在 C 语言中，全局变量如果不显式初始化，其默认值为______。

**32.** 若有 `int a[5] = {1, 2, 3, 4, 5}, *p = a + 3;`，则 `p[-1]` 的值是______。

**33.** 在单链表中，在 p 所指结点之后插入 s 结点的操作是：`s->next = ______; p->next = s;`。

**34.** 对 n 个结点的二叉树进行中序遍历，其时间复杂度为______。

**35.** 希尔排序是______排序算法的一种改进。

---

## 四、简答题（每小题 10 分，共 40 分）

**36.** 简述文件读写中 `fprintf` / `fscanf` 与 `fwrite` / `fread` 的区别。

**37.** 简述循环队列设计的目的及其判空判满方法。

**38.** 简述哈夫曼树的构造过程及其应用。

**39.** 简述哈希查找中解决冲突的两种常用方法（开放地址法和链地址法）。

---

## 五、计算题（每小题 10 分，共 30 分）

**40.** 写出以下程序的运行结果：

```c
#include <stdio.h>
void swap(int *a, int *b) {
    int temp = *a;
    *a = *b;
    *b = temp;
}
int main() {
    int x = 5, y = 10;
    int *p = &x, *q = &y;
    swap(p, q);
    printf("x=%d, y=%d\n", x, y);
    printf("*p=%d, *q=%d\n", *p, *q);
    return 0;
}
```

**41.** 给定二叉树的前序遍历序列为 `ABDEGCFH`，中序遍历序列为 `DBGEACHF`，请画出该二叉树，并写出后序遍历序列。

**42.** 给定无向图 G 的边集如下（顶点 1~6，每条边 `(u, v, w)` 表示顶点 u 到 v 的权值为 w）：

```
(1,2,5)  (1,3,3)  (2,3,4)  (2,4,6)
(2,5,2)  (3,5,7)  (4,5,1)  (4,6,9)
(5,6,8)
```

请分别使用 Prim 算法（从顶点 1 开始）和 Kruskal 算法求最小生成树，并写出两种算法得到的边序列。

---

## 六、应用题（每小题 10 分，共 30 分）

**43.** 编写程序，输入一个整数 n（1 ≤ n ≤ 20），输出 n 的阶乘（使用递归函数实现）。

**44.** 编写程序，定义一个学生结构体 `struct Student`（包含学号、姓名、三门课成绩），从键盘输入 3 个学生的信息，计算每个学生的平均成绩，并输出平均成绩最高的学生信息。

**45.** 编写程序，实现以下功能：
- 定义一个包含 10 个整数的数组
- 从键盘输入 10 个整数
- 采用直接插入排序法将其按升序排列
- 输出排序后的结果