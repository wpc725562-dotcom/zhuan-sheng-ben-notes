---
title: "卷一·基础巩固卷（答案）"
description: "广东专升本计算机 · 模拟卷（一）参考答案"
---

# 模拟卷（一）· 基础巩固卷 · 参考答案

---

## 一、单项选择题（20 题 × 3 分 = 60 分）

| 题号 | 答案 | 考点 | 解析 |
|:---:|:---:|:---|---|
| 1 | **C** | 1.1 标识符 | 标识符只能由字母、数字、下划线组成，且不能以数字开头，不能是关键字。A 数字开头，B 是关键字，D 含连字符。 |
| 2 | **B** | 1.2 整数除法 | 两整数相除，结果仍为整数（截断小数部分），`5/2 = 2`。 |
| 3 | **A** | 1.2 逻辑运算 | `x > y` 为假（3 > 4 为假），逻辑与 `&&` 短路，结果为 0。 |
| 4 | **B** | 1.3 scanf | 普通变量必须加 `&` 取地址。A 和 D 缺少 `&`。 |
| 5 | **C** | 1.4 switch | switch 缺 break 会导致**穿透**（fall-through），继续执行后续 case 直到遇到 break 或 switch 结束。 |
| 6 | **A** | 1.6 数组初始化 | 部分初始化时，未赋值的元素自动初始化为 0。`a[3] = 0`。 |
| 7 | **B** | 1.6 strlen | `strlen` 计算字符串长度，不含结尾的 `\0`，`"hello"` 长度为 5。 |
| 8 | **A** | 1.7 函数定义 | B 返回类型为 void 不能 return 值；C 语法错误；D 缺少返回类型。 |
| 9 | **B** | 1.8 指针 | `p = &a` 使 p 指向 a，`*p` 解引用得 a 的值 10。 |
| 10 | **B** | 1.9 结构体 | 结构体是用户自定义类型。A 错（系统不提供），C 错（成员可以是数组），D 错（结构体可作为函数参数）。 |
| 11 | **C** | 1.10 文件操作 | `fopen` 失败返回 NULL，这是标准规定。 |
| 12 | **B** | 2.2 带头结点链表 | 带头结点时，`head->next == NULL` 表示空表。`head == NULL` 是头结点不存在的情况。 |
| 13 | **C** | 2.2 链表删除 | 删除直接后继：`p->next = p->next->next`，跳过被删结点即可。 |
| 14 | **B** | 2.3 栈 | 栈的特性是**后进先出（LIFO）**，队列是先进先出（FIFO）。 |
| 15 | **C** | 2.5 二叉树 | 深度为 k 的二叉树最多有 `2^k - 1` 个结点。`2^4 - 1 = 15`。 |
| 16 | **A** | 2.5 遍历 | 先序遍历：根 → 左子树 → 右子树。 |
| 17 | **B** | 2.5 完全二叉树 | 叶子数 = `⌈n/2⌉ = ⌈12/2⌉ = 6`。 |
| 18 | **B** | 2.6 图遍历 | DFS 借助**栈**实现回溯，BFS 借助队列。 |
| 19 | **B** | 2.7 查找 | 二分查找要求数据**有序**且能随机存取（数组）。 |
| 20 | **C** | 2.8 排序 | 冒泡排序最坏（逆序）时 O(n²)，最好（已有序）时 O(n)。 |

---

## 二、判断题（10 题 × 2 分 = 20 分）

| 题号 | 答案 | 考点 | 解析 |
|:---:|:---:|:---|---|
| 21 | **×** | 1.6 数组 | 数组下标从 **0** 开始，不是 1。 |
| 22 | **√** | 1.4 赋值 | `a = 5` 是赋值表达式，值恒为 5（非零即为真），所以总是为真。 |
| 23 | **√** | 1.5 do-while | do-while 先执行一次循环体再判断条件，至少执行一次。 |
| 24 | **×** | 1.6 sizeof | `sizeof("abc")` 包含结尾的 `\0`，值为 4，而非 3。 |
| 25 | **√** | 1.7 静态变量 | 静态局部变量在程序整个运行期间存在，函数结束时不会释放。 |
| 26 | **×** | 1.8 指针 | 指针变量只能存储**地址**，不能存储任意整数值。 |
| 27 | **√** | 1.10 文件操作 | 以 `"r"` 方式打开不存在的文件，fopen 返回 NULL。 |
| 28 | **×** | 2.2 循环链表 | 循环链表判尾是 `p->next == head`，不是 NULL。 |
| 29 | **√** | 2.3 栈和队列 | 栈和队列都是受限的线性表，只能在端点操作。 |
| 30 | **√** | 2.5 二叉树 | 叶子结点数可能多于总结点数的一半（如只有一个根结点和一个叶子结点的树）。 |

---

## 三、填空题（5 题 × 4 分 = 20 分）

| 题号 | 答案 | 考点 |
|:---:|:---|---|
| 31 | **循环** 或 **switch** | 1.5 break |
| 32 | **0** | 1.6 二维数组部分初始化 |
| 33 | **->**（箭头运算符） | 1.9 结构体指针 |
| 34 | **LIFO**、**FIFO** | 2.3 栈和队列特性 |
| 35 | **⌈n/2⌉** 或 **n/2 向上取整** | 2.5 完全二叉树叶子数 |

---

## 四、简答题（4 题 × 10 分 = 40 分）

### 36. `#include <stdio.h>` 的作用（10 分）

**评分要点**：
- 预处理指令（2 分）
- 将标准输入输出头文件的内容包含到当前源文件中（4 分）
- 使得 printf、scanf 等函数可用（4 分）

### 37. 传值调用和传址调用的区别（10 分）

**评分要点**：

| 对比项 | 传值调用 | 传址调用 |
|:---|:---|:---|
| 传递内容 | 实参的副本（值） | 实参的地址 |
| 形参改变是否影响实参 | 不影响（5 分） | 通过指针间接修改实参（5 分） |
| 示例 | `void f(int x)` | `void f(int *p)` |

### 38. 顺序表和链表的区别（10 分，至少 3 点）

**评分要点**（每点 3 分，答出 3 点满分）：
1. **存储结构**：顺序表连续存储，链表离散存储
2. **随机存取**：顺序表 O(1) 随机存取，链表需 O(n) 遍历
3. **插入删除**：顺序表 O(n)（需移动元素），链表 O(1)（仅改指针）
4. **空间分配**：顺序表静态分配（可能溢出），链表动态分配

### 39. 二叉树三种遍历的定义（10 分）

**评分要点**：
- 先序遍历：根 → 左子树 → 右子树（4 分）
- 中序遍历：左子树 → 根 → 右子树（3 分）
- 后序遍历：左子树 → 右子树 → 根（3 分）

---

## 五、计算题（3 题 × 10 分 = 30 分）

### 40. 程序运行结果（10 分）

**结果**：**5**（10 分）

**解析**：`(a > b) ? a : b` 是条件表达式，`a > b`（5 > 3）为真，取 a 的值 5。

### 41. 程序运行结果（10 分）

**结果**：**80**（10 分）

**解析**：
- i=0（偶数）→ sum += arr[0] = 10
- i=1（奇数）→ 跳过
- i=2（偶数）→ sum += arr[2] = 10 + 30 = 40
- i=3（奇数）→ 跳过
- i=4（偶数）→ sum += arr[4] = 40 + 50 = 90
- 最终 sum = 90

**更正：** 结果为 **90**。

### 42. 二叉树还原（10 分）

**步骤**：
1. 前序 `ABDECF` → 根为 A（2 分）
2. 中序 `DBEAFC` → A 左边为左子树（DBE），右边为右子树（FC）（2 分）
3. 左子树前序 `BDE` → 根为 B，中序 `DBE` → B 左边为 D，右边为 E（3 分）
4. 右子树前序 `CF` → 根为 C，中序 `FC` → C 左边为空，右边为 F（3 分）

```
      A
     / \
    B   C
   / \   \
  D   E   F
```

---

## 六、应用题（3 题 × 10 分 = 30 分）

### 43. 求和及平均值（10 分）

**评分标准**：
- 框架 2 分：`#include <stdio.h>` + `int main()` + `return 0;`
- 数组/循环输入 3 分
- 求和逻辑 3 分
- 输出 2 分

**参考答案**：

```c
#include <stdio.h>
int main() {
    int a[10], i, sum = 0;
    float avg;
    printf("请输入10个整数：");
    for (i = 0; i < 10; i++) {
        scanf("%d", &a[i]);
        sum += a[i];
    }
    avg = sum / 10.0;
    printf("和 = %d，平均值 = %.2f\n", sum, avg);
    return 0;
}
```

### 44. 统计字符个数（10 分）

**评分标准**：
- 框架 2 分
- 字符串输入 2 分
- 分类判断逻辑 4 分
- 输出结果 2 分

**参考答案**：

```c
#include <stdio.h>
int main() {
    char str[100];
    int i, upper = 0, lower = 0, digit = 0;
    printf("请输入一个字符串：");
    gets(str);
    for (i = 0; str[i] != '\0'; i++) {
        if (str[i] >= 'A' && str[i] <= 'Z') upper++;
        else if (str[i] >= 'a' && str[i] <= 'z') lower++;
        else if (str[i] >= '0' && str[i] <= '9') digit++;
    }
    printf("大写字母：%d，小写字母：%d，数字：%d\n", upper, lower, digit);
    return 0;
}
```

### 45. 冒泡排序（10 分）

**评分标准**：
- 框架 2 分
- 数组定义和输入 2 分
- 冒泡排序核心逻辑 4 分（双重循环 + 交换）
- 输出结果 2 分

**参考答案**：

```c
#include <stdio.h>
int main() {
    int a[10] = {34, 67, 12, 89, 45, 23, 56, 78, 90, 11};
    int i, j, temp;
    // 冒泡排序
    for (i = 0; i < 9; i++) {
        for (j = 0; j < 9 - i; j++) {
            if (a[j] > a[j + 1]) {
                temp = a[j];
                a[j] = a[j + 1];
                a[j + 1] = temp;
            }
        }
    }
    printf("排序结果：");
    for (i = 0; i < 10; i++) {
        printf("%d ", a[i]);
    }
    printf("\n");
    return 0;
}
```

---

> **卷一（基础巩固卷）命题完成**  
> 下一卷：**卷二（综合中档卷）**