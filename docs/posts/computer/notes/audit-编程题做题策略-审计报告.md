# 章节审计报告 · 编程题做题策略专项文档

> **审计模式**：仅审计，不生成讲义正文（新建文档）
> **审计日期**：2026-08-16
> **数据源**：历年真题/计算机程序设计/2021.md ~ 2024.md

---

## 一、历年编程题完整统计（4年，每题10分，共30分/年）

| 年份 | 题号 | 题型 | 具体考点 | 难度 |
|:---:|:---:|:---:|---|:---:|
| 2021 | 43 | 编程 | 指针实现交换两数 + 输出较大值 | ⭐⭐ |
| 2021 | 44 | 编程 | 遍历字符串，剔除非数字字符 | ⭐⭐⭐ |
| 2021 | 45 | 编程 | Prim 最小生成树（过程描述） | ⭐⭐⭐⭐ |
| 2022 | 1 | 编程 | DFS 遍历序列 + 最小生成树（算法名+画树） | ⭐⭐⭐⭐ |
| 2022 | 2 | 编程 | 统计字符串中指定字符个数 | ⭐⭐ |
| 2022 | 3 | 编程 | 选择排序 30 个成绩（代码填空） | ⭐⭐⭐ |
| 2023 | 1 | 编程 | 三次成绩取最大 | ⭐ |
| 2023 | 2 | 编程 | 判断正负零 | ⭐ |
| 2023 | 3 | 编程 | 一维数组查找（十班人数查 y） | ⭐⭐ |
| 2024 | 1 | 编程 | 201~209 存入 3×3 二维数组并输出矩阵 | ⭐⭐ |
| 2024 | 2 | 编程 | 30 名学生成绩求最高分 | ⭐⭐ |
| 2024 | 3 | 编程 | 1~300 奇数和与偶数和分别输出 | ⭐⭐ |

> **4年合计：12题，年均3题，每题10分，合计30分/年**
> 出题率：★★★★★（大题必考，占卷面15%）

---

## 二、考点类型分布

| 考点类别 | 出现次数 | 比例 | 年份分布 |
|:---|---:|:---:|:---|
| **数组遍历/操作** | 5 | 42% | 2021·44, 2022·3, 2023·3, 2024·1, 2024·2 |
| **条件判断/分支** | 3 | 25% | 2021·43, 2023·1, 2023·2 |
| **图算法** | 2 | 17% | 2021·45, 2022·1 |
| **字符串处理** | 1 | 8% | 2022·2 |
| **循环累加** | 1 | 8% | 2024·3 |

---

## 三、编程题常见模式模板

### 模式1：数组遍历 + 求最值（最高频！）

```c
// 2024·30人最高分 / 2023·三次取最大
int max = a[0];                 // 假设第一个最大
for (int i = 1; i < n; i++) {
    if (a[i] > max) max = a[i]; // 发现更大就更新
}
```

### 模式2：数组遍历 + 查找

```c
// 2023·十班人数查 y
int found = 0;
for (int i = 0; i < n; i++) {
    if (a[i] == target) { found = 1; break; }
}
```

### 模式3：二维数组遍历

```c
// 2024·201~209 存入 3×3 矩阵
int num = 201;
for (int i = 0; i < 3; i++) {
    for (int j = 0; j < 3; j++) {
        a[i][j] = num++;
    }
}
```

### 模式4：条件判断 + 指针

```c
// 2021·交换 a、b 并输出较大值
void swap(int *x, int *y) { int t = *x; *x = *y; *y = t; }
// 调用后 if (a > b) printf("%d", a); else printf("%d", b);
```

### 模式5：字符串遍历

```c
// 2021·剔除非数字字符 / 2022·统计字符个数
while (*s) {
    if (isdigit(*s)) putchar(*s);  // 或：if (*s == ch) count++;
    s++;
}
```

### 模式6：排序算法

```c
// 2022·选择排序 30 个成绩
for (int i = 0; i < n-1; i++) {
    int k = i;
    for (int j = i+1; j < n; j++)
        if (a[j] < a[k]) k = j;
    if (k != i) { int t = a[i]; a[i] = a[k]; a[k] = t; }
}
```

---

## 四、评分标准解析（每题10分）

| 评分维度 | 分值 | 说明 |
|:---|---:|:---|
| 头文件 + main 框架 | 2 | `#include <stdio.h>` + `int main()` + `return 0` |
| 输入/赋值 | 2 | `scanf` 或初始化 |
| 核心逻辑 | 4 | 算法的核心实现（循环/条件/指针等） |
| 输出 | 2 | `printf` 输出结果 |

---

## 五、高频丢分陷阱

| 陷阱类型 | 具体表现 |
|:---|---|
| **忘记 return 0** | main 函数末尾没写 `return 0;` |
| **scanf 忘记 &** | `scanf("%d", a)` 数组名不用 &，但 `scanf("%d", &n)` 普通变量要 & |
| **数组下标越界** | 长度为 n 的数组，最多访问 `a[n-1]`，不是 `a[n]` |
| **字符串无 \0** | 字符数组初始化时忘记字符串结束符 |
| **指针参数传错** | 函数参数是 `int *x`，调用时传 `&a` 不是 `a` |
| **循环边界错误** | `for (i=0; i<=n; i++)` 多循环一次 |

---

## 六、笔记改造优先级建议

### P0（新建文档必备）

| 优先级 | 内容 | 说明 |
|:---:|---|---|
| **P0** | 编程题通用模板框架 | 6大常见模式 + 10分评分标准 |
| **P0** | 历年真题嵌入（6道代表题） | 覆盖所有模式 |
| **P0** | 闭卷挑战（10题） | 从易到难练习 |

### P1（扩充）

| 优先级 | 内容 | 说明 |
|:---:|---|---|
| **P1** | 考点频次与趋势分析 | 指导复习方向 |
| **P1** | 评分标准与答题规范 | 如何拿满10分 |

---

## 七、结论

**当前状态：新建文档，需从零搭建**

编程题占卷面**30分**（15%），是必争分数。考点集中在：
1. **数组遍历/求最值**（42%）——最高频，必会
2. **条件判断/分支**（25%）——基础题，不能丢分
3. **图算法**（17%）——量少但难，需理解过程

**待你确认后，即按 P0→P1 顺序生成讲义正文。**