# 📅 每日刷题计划 · 2026-09-10

> 由 `scripts/daily-drill.mjs` 生成。共 **15** 题：基础 13 / 中档 2 / 拔高 0。
> 难度配比 基础:中档:拔高 = 6:3:1，题库与顺序随机（科目交错混排）。
> **零基础正确用法**：读题 → 想 30 秒 → **照抄完整解答 → 合上默写一遍**。做不出来是正常的，不要硬憋，也不要因为不会就跳过。

### 1. 【C语言】【改错题】 ★★ 中档

递归求阶乘（找出并改正下面代码中的错误）

```c
#include <stdio.h>

int factorial(int n)
{
    if (n == 0)
        return 0;   /**** 错误 1 ****/
    return n * factorial(n - 1);
}

int main()
{
    printf("%d\n", factorial(5));
    return 0;
}
```

> 🧭 **先想 30 秒**：逐行只查四件事：分号、== 与 =、scanf 的 &、数组下标越界。　👉 想满 30 秒还不动，就展开下面的解答照抄一遍，合上再默写。

<details>
<summary>💡 看完整解答与讲解</summary>

**答案**：`return 0;` → `return 1;`

**解析**：

0! = 1（数学定义），不是 0。若返回 0，整个阶乘结果都为 0。


</details>

### 2. 【C语言】 ★ 基础

`while` 和 `do...while` 的区别？

> 🧭 **先想 30 秒**：这题是基础题，直接照抄解答再默写一遍即可。　👉 想满 30 秒还不动，就展开下面的解答照抄一遍，合上再默写。

<details>
<summary>💡 看完整解答与讲解</summary>

**完整解答（照着抄一遍，然后合上默写）**：

- `while (条件) {...}` → **先判断再执行**，条件不成立可以一次都不执行
- `do {...} while (条件);` → **先执行一次再判断**，**至少执行一次**（注意结尾有分号）

判断题高频："do-while 循环体至少执行一次" → **对**。


</details>

### 3. 【C语言】【编程题】 ★ 基础

判断一个数是否为素数

```c
int main() {
    int n, i, flag = 1;
    scanf("%d", &n);
    for (i = 2; i * i <= n; i++)    /* 只需试到 √n */
        if (n % i == 0) { flag = 0; break; }
    if (flag && n > 1) printf("是素数\n");
    else               printf("不是素数\n");
    return 0;
}
```

> 🧭 **先想 30 秒**：这题是基础题，直接照抄解答再默写一遍即可。　👉 想满 30 秒还不动，就展开下面的解答照抄一遍，合上再默写。

<details>
<summary>💡 看完整解答与讲解</summary>

**答案**：以 n=17 为例，i 试到 2、3、4 后结束，输出"是素数"。

**解析**：

① 循环边界写成 `i < n` 也能对但效率低，写成 `i <= n/2` 同理；② n=1、0 必须判"不是素数"（1 不是素数，真题爱挖这个坑）。


</details>

### 4. 【高等数学】 ★ 基础

`x → 0⁺` 和 `x → 0⁻` 有什么区别？

> 🧭 **先想 30 秒**：这题是基础题，直接照抄解答再默写一遍即可。　👉 想满 30 秒还不动，就展开下面的解答照抄一遍，合上再默写。

<details>
<summary>💡 看完整解答与讲解</summary>

**完整解答（照着抄一遍，然后合上默写）**：

- `0⁺`：**从右边靠近 0**（比 0 大一点点），如 0.001
- `0⁻`：**从左边靠近 0**（比 0 小一点点），如 -0.001

区别在 `1/x` 上特别明显：`x→0⁺` 时 `1/x → +∞`；`x→0⁻` 时 `1/x → -∞`。

**左右极限不相等 → 极限不存在**，这是判断题最爱考的点。


</details>

### 5. 【计算机】【C语言部分】 ★ 基础

以下程序输出是（ ）

```c
int arr[] = {1, 2, 3, 4, 5};
int *ptr = arr;
ptr++;
printf("%d", *ptr);
```

A. 1　　B. 2　　C. 3　　D. 地址值

> 🧭 **先想 30 秒**：这题是基础题，直接照抄解答再默写一遍即可。　👉 想满 30 秒还不动，就展开下面的解答照抄一遍，合上再默写。

<details>
<summary>💡 看完整解答与讲解</summary>

**答案**：B

**解析**：

`ptr` 初始指向数组首元素（`arr[0]`=1）。`ptr++` 使指针向后移动一个 `int` 大小（4字节），指向 `arr[1]`，所以 `*ptr` 为 2。

指针就像游戏里的"角色箭头"——一开始指向第1个人，`ptr++` 就是"箭头向右移一位"，指向第2个人。移动的距离取决于指向的东西有多大（`int` 占4个位置）。


</details>

### 6. 【C语言】【改错题】 ★ 基础

数组求最大值（找出并改正下面代码中的错误）

```c
#include <stdio.h>

int findMax(int a[], int n)
{
    int i, max = 0;   /**** 错误 1 ****/
    for (i = 1; i < n; i++)
        if (a[i] > max)
            max = a[i];
    return max;
}

int main()
{
    int arr[5] = {-3, -1, -5, -2, -4};
    printf("%d\n", findMax(arr, 5));
    return 0;
}
```

> 🧭 **先想 30 秒**：这题是基础题，直接照抄解答再默写一遍即可。　👉 想满 30 秒还不动，就展开下面的解答照抄一遍，合上再默写。

<details>
<summary>💡 看完整解答与讲解</summary>

**答案**：`max = 0` → `max = a[0]`，且循环从 `i = 1` 开始（或 `max = a[0]; for(i=0; i<n; i++)`）

**解析**：

当数组全为负数时，初始化为 0 会导致返回 0 而非真正的最大值（-1）。应初始化为数组第一个元素。


</details>

### 7. 【C语言】【编程题】 ★ 基础

求 5!（阶乘）

```
输出样例：120
```

```c
int main() {
    int i;
    long fact = 1;                  /* 阶乘增长快，用 long 更稳 */
    for (i = 1; i <= 5; i++)
        fact *= i;
    printf("%ld\n", fact);
    return 0;
}
```

> 🧭 **先想 30 秒**：这题是基础题，直接照抄解答再默写一遍即可。　👉 想满 30 秒还不动，就展开下面的解答照抄一遍，合上再默写。

<details>
<summary>💡 看完整解答与讲解</summary>

**答案**：见下方完整解答


</details>

### 8. 【C语言】【运算符优先级与表达式求值】 ★ 基础

以下程序输出是（ ）

```c
int a[10];
printf("%d\n", sizeof(a) / sizeof(int));
```

A. 10 B. 4 C. 40 D. 1

> 🧭 **先想 30 秒**：这题是基础题，直接照抄解答再默写一遍即可。　👉 想满 30 秒还不动，就展开下面的解答照抄一遍，合上再默写。

<details>
<summary>💡 看完整解答与讲解</summary>

**答案**：A

**解析**：

`sizeof(a)` = 40（10×4），`sizeof(int)` = 4，40/4 = 10。这是计算数组长度的常用写法。


</details>

### 9. 【高等数学】【连续性·间断点】 ★ 基础

判断函数 $f(x) = \frac{x^2 - 1}{x^2 - 3x + 2}$ 的间断点及其类型。

> 🧭 **先想 30 秒**：这题是基础题，直接照抄解答再默写一遍即可。　👉 想满 30 秒还不动，就展开下面的解答照抄一遍，合上再默写。

<details>
<summary>💡 看完整解答与讲解</summary>

**完整解答（照着抄一遍，然后合上默写）**：

分母 $x^2 - 3x + 2 = (x - 1)(x - 2)$，间断点为 $x = 1$ 和 $x = 2$。

**（1）$x = 1$ 处：**

$$
\lim_{x \to 1} f(x) = \lim_{x \to 1} \frac{(x - 1)(x + 1)}{(x - 1)(x - 2)}
= \lim_{x \to 1} \frac{x + 1}{x - 2} = \frac{2}{-1} = -2
$$

极限存在且为 $-2$，但 $f(1)$ 无定义，故 $x = 1$ 为**可去间断点**。

**（2）$x = 2$ 处：**

$$
\lim_{x \to 2} f(x) = \lim_{x \to 2} \frac{x + 1}{x - 2}
$$

当 $x \to 2^+$ 时，$\frac{x + 1}{x - 2} \to +\infty$；当 $x \to 2^-$ 时，$\frac{x + 1}{x - 2} \to -\infty$。
极限不存在，故 $x = 2$ 为**第二类无穷间断点**。

**这题在考什么**：

间断点分类（可去间断点、跳跃间断点、第二类间断点）


</details>

### 10. 【高等数学】 ★ 基础

`Σ` 是什么符号？`Σ(n=1到4) n` 等于多少？

> 🧭 **先想 30 秒**：这题是基础题，直接照抄解答再默写一遍即可。　👉 想满 30 秒还不动，就展开下面的解答照抄一遍，合上再默写。

<details>
<summary>💡 看完整解答与讲解</summary>

**完整解答（照着抄一遍，然后合上默写）**：

`Σ` 是希腊字母 sigma，表示**求和**（加起来的缩写）。

`Σ(n=1到4) n` = 1 + 2 + 3 + 4 = **10**。

读法：「n 从 1 加到 4 的 n 之和」。下面写起点，上面写终点，右边写"每一项长什么样"。


</details>

### 11. 【C语言】【编程题】 ★★ 中档

求 1! + 2! + 3! + … + n!（n=4）

```
输出样例：33
```

```c
int main() {
    int i, n = 4;
    long fact = 1, sum = 0;
    for (i = 1; i <= n; i++) {
        fact *= i;      /* 在上一轮阶乘基础上乘 i，不必重算 */
        sum += fact;
    }
    printf("%ld\n", sum);
    return 0;
}
```

> 🧭 **先想 30 秒**：先默写最小框架 #include + int main + return 0，再往里填循环。　👉 想满 30 秒还不动，就展开下面的解答照抄一遍，合上再默写。

<details>
<summary>💡 看完整解答与讲解</summary>

**答案**：1+2+6+24 = **33**

**解析**：

把阶乘循环写成双层重新算——结果对但绕远；真题若限定"单层循环"双层就不得分。核心思想：**i! = (i−1)! × i**。


</details>

### 12. 【高等数学】【极限·两个重要极限】 ★ 基础

求极限 $$\lim_{x \to 0} \frac{\sin 5x}{2x}$$

> 🧭 **先想 30 秒**：这题是基础题，直接照抄解答再默写一遍即可。　👉 想满 30 秒还不动，就展开下面的解答照抄一遍，合上再默写。

<details>
<summary>💡 看完整解答与讲解</summary>

**完整解答（照着抄一遍，然后合上默写）**：

利用第一个重要极限 $\lim_{u \to 0} \frac{\sin u}{u} = 1$：

$$
\lim_{x \to 0} \frac{\sin 5x}{2x}
= \lim_{x \to 0} \frac{5}{2} \cdot \frac{\sin 5x}{5x}
= \frac{5}{2} \times 1
= \frac{5}{2}
$$

**这题在考什么**：

第一个重要极限 $\lim_{x \to 0} \frac{\sin x}{x} = 1$


</details>

### 13. 【政治】 ★ 基础

中国式现代化的本质特征是？
A. 人口规模巨大的现代化
B. 全体人民共同富裕的现代化
C. 坚持中国共产党领导
D. 物质文明和精神文明相协调的现代化

> 🧭 **先想 30 秒**：这题是基础题，直接照抄解答再默写一遍即可。　👉 想满 30 秒还不动，就展开下面的解答照抄一遍，合上再默写。

<details>
<summary>💡 看完整解答与讲解</summary>

**答案**：C

**解析**：

中国式现代化是中国共产党领导的社会主义现代化，党的领导是中国式现代化的本质特征和根本保证。


</details>

### 14. 【计算机】【C语言高频简答题】 ★ 基础

什么是算法？算法的特点有哪些？

> 🧭 **先想 30 秒**：这题是基础题，直接照抄解答再默写一遍即可。　👉 想满 30 秒还不动，就展开下面的解答照抄一遍，合上再默写。

<details>
<summary>💡 看完整解答与讲解</summary>

**完整解答（照着抄一遍，然后合上默写）**：

算法是解决特定问题的一系列有限步骤的集合，是解决问题的清晰指令描述。

算法的五大特点：
1. **有穷性**：算法必须在执行有限步后结束。
2. **确定性**：算法的每一步必须有确切的含义，无二义性。
3. **可行性**：算法中的操作都可以通过基本运算执行有限次实现。
4. **输入**：算法有零个或多个输入。
5. **输出**：算法有一个或多个输出，没有输出的算法无意义。


</details>

### 15. 【C语言】【改错题】 ★ 基础

交换两个数（找出并改正下面代码中的错误）

```c
#include <stdio.h>

void swap(int a, int b)   /**** 错误 1 ****/
{
    int temp;
    temp = a;
    a = b;
    b = temp;
}

int main()
{
    int x = 3, y = 5;
    swap(x, y);
    printf("%d, %d\n", x, y);
    return 0;
}
```

> 🧭 **先想 30 秒**：这题是基础题，直接照抄解答再默写一遍即可。　👉 想满 30 秒还不动，就展开下面的解答照抄一遍，合上再默写。

<details>
<summary>💡 看完整解答与讲解</summary>

**答案**：`void swap(int a, int b)` → `void swap(int *a, int *b)`

**解析**：

C 语言传值调用，形参改变不影响实参。必须用指针传递地址才能真正交换。


</details>

> 分布：C语言 8 · 高等数学 4 · 计算机 2 · 政治 1

共 15 题。错题登记：docs/checklists/错题本模板.md
重抽一次（换题）：node scripts/daily-drill.mjs --total 15