# 📊 学习进度看板（AI 交接单一事实源）

> 🤖 **本文件是 AI 教练跨会话交接的核心文件**——每次学习会话结束时由教练更新，新会话开始时读取。
> 👤 **给你**：也可以手动编辑任何区块，AI 以下次会话读取到的最新内容为准。
> 🔄 **更新协议**：每次学习会话结束前，AI 必须刷新「上次会话」「当前状态」「本周任务」三区，其余区按需更新。`last_updated` 是新会话判断信息新鲜度的依据。

**last_updated**: 2026-09-13 00:25
**考试倒计时**: 距 2027-03 约 6 个月

---

## 🎯 一句话当前状态

**⚠️ 09-08~09-09 大部分时间仍在磨工具（门户/脚本），看板原警告的「完善工具式伪勤奋」确实发生了。**
**但 09-09 出现了第一次真实作答**：C 语言最小框架 0/20 → **20/20（默写全对）**；高数 4 题 **20/40**。唯一缺口非常明确——**「会读符号、不会算结果」**：题 2 只把条件翻译成人话就停了（没算出值），题 4 判断对但说不出原因（按置信度封顶记 0.5，不算掌握）。
弹药已上膛：门户「今日刷题」= KaTeX 真数学 + 读题教练人话讲解 + 答案二次折叠逼回想 + 全题库加权随机；另新增「今日三步」面板与 Anki 真实进度面板（英语显式降为保鲜轨）。
**09-09 深夜～09-10 凌晨（第二轮实测）：那 3 道欠交题已全部交回且全对**，加上补做的 ④⑤，**高数 6 题 100/100**（同考点上一轮是 50%）——「会读符号、不会算结果」这个缺口**已闭合**。题 ③ 的理由是他自己的反问句（"2x 无限趋近于无穷大怎么会等于 0"），不是背的。

**但 C 语言立刻撞新墙，且根子在教练身上**：人肉编译 `for(i=1;i<=4;i++) s=s+i;` 他答 **4**（正确 10）——把计数器 `i` 的最后值当成累计器 `s`，还以为循环只跑 1 轮。他当场指出「你是不是忘了我都是零基础」，**这条反馈成立**：教练只教了三行框架就直接抛 for 循环，变量/赋值根本没教。→ 立为教学纪律：**一次只给一个新概念，没教过的不出题。**

**🔴 假掌握警报 → 09-10 凌晨已解除**：先「有表格填 `1→3→6→10`」全对、「撤表格做 `a=a+2; a=a*2;`」答 6（假掌握）；随后**合上讲义无表格重做答 10 ✅**，同型新题 `x=10; x=x-4; x=x+1; x=x*2;` 答 **14 ✅**（10→6→7→14）→ **同型连对 2 次，「赋值覆盖 / `=` 从右往左」这一格真过关**（#C001 复习记录已签）。

**🆕 但立刻撞上一个更该修的病：答案从上一题借。** ③ `for(i=1;i<=3;i++) s=s+i;` 答 **10**（正确 **6**）——上一题 `i<=4` 恰好是 10，他**没算就把上题答案搬过来，而且没交表格**。这不是粗心，是"跳过计算直接凑数"，考场上必死。已登记 **#C002**，并立**新判据：只给数字不给过程 = 一律按错题重做。**

**下次开课第一件事（三样，都要交过程）**：① 重做 #C002（`i<=3` 版，**必须画 3 行表格**，答案 6）② ④ 花括号范围题（循环内 A/B、循环外 C，`i<=2` → 正确 **A B A B C** 共 5 行）③ `x→0` 分岔口实测：只出一道 $\lim\limits_{x\to 0}\frac{1}{x}$，答 0 就是把 $x\to\infty$ 规则硬套（回炉 1.11 讲义第四节）。**工具继续封刀。**

**⚠️ 封刀戒律本轮已破（09-10 凌晨，诚实记录）**：又建了门户「🪞 复盘」+「📐 公式台」两个标签页、接了 `学习日志.jsonl` 接口、顺手修了 favicon 404。**这些是弹药，不是学习**。三件事已做完（日志沉淀 / 公式可视化 / 英语正确率叫停提醒），**即刻再封刀，下次会话不开任何工具类任务**。真进度只有科目进度条那 8%，下面这些页面一行都不算。

**📇 Anki 英语高频词快照（09-12 17:30 实测，直读 `collection.anki2` 的 revlog 操作日志，不认自报）**

| 项目 | 数值 | 人话 |
|:---|:---|:---|
| 词库总量 | 2230 张 | 已开课 991（**44.4%**）· 从未碰过 1239 |
| 今日到期 | 180 张 | 测量时**今日做题 0 次** —— 一张都还没动 |
| 逾期最久 | **22 天** | 最早一批到期卡拖了三周，它不会自己消失 |
| 记得牢（间隔≥15天） | 334 张 | 其中间隔≥30天的只有 **138 张** |
| 忘过至少一次 | 59 张 | |
| 全历史做题 | 3734 次 / **3.8 小时** | 平均 3.7 秒一次 —— 手很快，多半在"认脸" |
| 全历史正确率 | 61.2% | 近 7 天 2874 次、**54.2%**（在往下掉） |
| 按钮分布 | 困难 **33.2%** · 重来 5.7% · 保留率 94.3% | 见下方判读 |

**判读两条，都可核对**：
1. **困难按到 33.2%、重来只有 5.7% —— 你几乎没有"真忘掉"的词，是自己把词按成"难"的。** 每点一次困难，Anki 就把这张卡的聪明值永久砍一刀，砍完它就被排得更频繁。这是**亲手制造假难词**，不是记忆力差。改法一句话：**想起来了就点「好的」，「困难」只留给"看了答案才想起来"的那种。**（保留率 94.3% 说明排期本身没坏，问题在手不在算法。）
2. **开课率 40%（09-10）→ 44.4%（09-12），两天涨约 100 张新词**（09-07 一天就开了 788 张）。全历史**新学 : 复习 = 1.8 : 1**，而零基础建词库这个比例**应该反过来**。新词开太快，复习债就滚成今天这 180 张到期 + 22 天逾期。
   **药方**：先按每天 20-30 张清 22 天的欠账（约 41-62 天追上），期间**每天新词不超过 20 个**；做完就收手，别为清库硬刷。

---

## 📌 当前阶段（打地基：2026-09 上旬 - 11 月底）

| 事项 | 状态 |
|:---|:---|
| 主线目标 | C 语言推到数组/函数/指针；高数推到导数与求导法则 |
| 每日配额 | 4-5h（高数 2h + C 语言 2-3h 交错）+ Anki 英语词 15min |
| 科学规则 | 提取练习优先（先做题再讲解）、交错出题、错题进 Anki `专升本::错题集` |
| ⚠️ 风险 | 截至本次会话**还没做过任何一道题**——警惕"囤资料/完善工具"式伪勤奋 |

## ✅ 上次会话完成的事（2026-09-09 下午～晚间）

### 学习事件（本会话首次出现，20 分钟微课 + 6 题实测）

用户明确否决"继续做工具"（"这些知识我还一个都没学，怎么写"），会话转入教学。改上 20 分钟微课（5 样东西：lim 读法 / x→0 与 x→∞ / ∞ 不是数 / C 最小框架 / 整数除法），随后实测：

| 项目 | 得分 | 判定 |
|:---|:---:|:---|
| C 语言最小框架默写 | **20/20** | ✅ 掌握（09-07 是 0/20，框架残缺已消除） |
| 高数极限 4 题（读+算） | **20/40** | ⚠️ 未掌握，缺口极明确：**会读符号、不会算结果** |

- 题 2 只把 `lim(x→∞) 1/x` 翻译成人话就收笔，没写出"= 0" → 按 DeepTutor 的 `error_diagnosis` 记**应用错误型**
- 题 4 判断正确但不说原因 → 按 `mastery.py` 的 `_CONFIDENCE_CAP`（confidence=2 封顶 0.8）只记 0.5，**不算掌握**
- 书写细节：把「无限」写成「无线」；`printf("wpc ")` 引号内多一个空格 → 已立规矩：输出要**逐字符自检**（考场没有编译器替他报错）
- 原 09-09 那 15 题计划**全部作废不做**（对当时的他量级不对），已改为此 20 分钟微课
- ~~**欠交 3 题**~~ → **09-09 深夜已全部交回，见下方第二轮实测**

### 第二轮实测（09-09 深夜 → 09-10 凌晨，同一会话续测）

| 项目 | 得分 | 判定 |
|:---|:---:|:---|
| 高数极限 6 题（含欠交 3 题 + 抓大头 2 题 + 判断说理 1 题） | **100/100** | ✅ 上一轮的「会读符号不会算结果」缺口**已闭合**（同考点 50% → 100%） |
| C 语言 for 循环人肉编译 | 高数满分同时 **0/40** | ❌ 撞新墙，且根因在教练（见下） |

- 题 ① `lim(x→∞) 5/x = 0` ✅ ② `lim(x→∞) (1/x+3) = 3` ✅ —— **这次写出了等号和一个数**，正是上一轮缺的那一步
- 题 ③ 判断"错"+ 理由「2x 无限趋近于无穷大怎么会等于 0」✅ —— 用自己的反问句说出来的，不是背术语，判 passed
- 题 ④⑤（抓大头同型 `\frac{2x+1}{x}`=2、`\frac{5x-3}{x+2}`=5）✅ 秒答，配角项 `+1`/`-3`/`+2` 正确无视
- ❌ **⑥ for 循环答 4（正确 10）**：把计数器 `i` 的末值当累计器 `s`，且以为循环只跑 1 轮 → 典型**未教先考**。他当场反馈「你是不是忘了我都是零基础」，**成立**
- ✅ **A 填表（有脚手架）全对 1→3→6→10**，❌ **B 独立做 `a=a+2; a=a*2;` 答 6（正确 10）** → **假掌握警报**：撤掉脚手架就错，说明"会"只存在于表格里
- ⚠️ ③C 题（`i<=3` 版）**未交**；④花括号范围题**未交** → 进待验证区
- 📌 教学纪律新增：**一次只教一个概念；没教过的东西不出题**（本会话唯一失误归教练，不归学生）

### 工具侧产出（弹药，不是学习；本轮已封刀）

工具侧产出（弹药，不是学习）：
- 门户 `D:\studio`：KaTeX 本地化（`vendor/katex`，1.5M，离线可用）+ 公式已实测渲染成真数学；`/api/drill` 改用 `--total`（原来误用 `--count 5` → 9 源出 45 题且绕过随机分布）；新增 `POST /api/drill/reshuffle`「换一批题」
- 出题脚本 `scripts/daily-drill.mjs`：`distribute()` 修掉补齐偏置 + 加 `BANK_WEIGHT` 加权（高数/C编程权重 3，政治/简答 1，落实"数计主战场、英语降权"）；**答案改为默认折叠**（旧 `--closed` 语义反了，重命名为 `--open`）
- 读题教练：新增「n 项和的极限 · 夹逼准则」一类（上下两道墙三步套路 + 两大失效信号 + 不得分写法），对应 09-09 那道真题
- ~~`plan/每日刷题-2026-09-09.md`：15 题手写版~~ → **晚间全部作废不做**（对当时零基础的量级不对），已改为 20 分钟微课 + 6 题实测
- 晚间补齐：`anki.mjs` **已接进 `GET /api/anki`**（CJS 动态 import + 60s 缓存 + in-flight 锁）；门户新增「今日三步」与「Anki 卡片进度」两面板，英语牌组标为**保鲜轨**并视觉降权
- 两轮子 agent 交叉审查共修 10 处，含我自己引入的 4 个新缺陷：[blocker]「换一批」点了完全无响应（裸 `data-reshuffle` 是空串 falsy）、[high] 今日三步勾选跨日不清零会谎报"已交 3/3"、[high] 我新加的长度签名会碰撞致陈旧渲染、[medium] 并发请求互踩临时 sqlite；顺手删掉 Google Fonts 外链（原本违反离线约束）
- ✅ **门户已重启（09-10 凌晨，此条销账）**：`taskkill` 旧 PID + `nohup node server.js` 拉起，实测 `/`、`/api/anki`、`/api/log`、`/math/`、`/favicon.ico`、`/studio.css` **全部 200**。并用 Playwright 真机点开「🪞 复盘」「📐 公式台」验证渲染：复盘页 4 个区块数字全对、公式台 46 处 KaTeX 全渲染 0 残留。控制台唯一那条 error 是 `/favicon.ico` 404 → 已复用 `sakura.ico` 修掉（顺手给标签页上了樱花图标）
- 🆕 **新增「🪞 复盘」标签页**：数据源是 `备考计划/学习日志.jsonl`（逐行 JSON，服务端 `readStudyLog()` 解析，坏行跳过不报错），走 `GET /api/log`。四块：概览统计 / **已过关**（只认 `mastery=passed`）/ **待攻克**（`failed` + weak 标签云）/ 时间线。**判据写死在页面上**：撤掉讲义和表格后独立做对才算过关，有脚手架做对记「假掌握」不算进度。实测渲染：14 条记录 / 3 过关 / 4 待攻克 / 覆盖 4 天
- 🆕 **新增「📐 公式台」标签页**：同源挂载 `D:/math-notes`（`MOUNTS` 加 `/math` 前缀），把上一步做的离线公式页嵌进门户，`serveStatic` 对 `/` 结尾路径自动补 `index.html`。**为什么挂门户而不是走笔记站构建**：vitepress build 被门拦 + 要重启门户，而这份 HTML 自包含、离线、不需构建
- 🆕 `studio.css` 是**纯暗色主题**（`:root` 里 `--bg:#1a1620`），新页面配色要按暗色写，别照抄笔记站的浅色
- ⚠️ **本仓无 git 保护**（`D:/studio` 不是 git 仓库，`git fetch` 实跑失败）→ 改坏了没法回滚，每次动 `server.js/studio.js` 前后必跑 `node --check`

### 🔨 工具侧产出第二轮（09-12 ~ 09-13 凌晨）——**封刀戒律又破了，诚实记一笔**

**破刀事实**：本轮用户明确要求改门户（首屏砍到三块 / 背词页只留配额+背词本 / 家教要真能启动 / 掌握度跟踪），我照做了。**这些仍然是弹药，不是学习**。真进度只有下面「科目进度条」那 8%，掌握度清单上那 8 个考点**一个都没过关**。

- **新增 `D:/studio/focus.js` + `focus.css`**（首屏三块：👉下一步 / 📊掌握度清单 / 🍅计时）。算法**照抄 DeepTutor 源码**不自己发明，出处都标在常量注释里：近因权重 `RECENT_W=[0.5,0.7,0.85,0.95,1.0]`（`learning/mastery.py:17`）、置信度封顶 `CONF_CAP={1:0.5,2:0.8}`（`mastery.py:21`，专治"做对一题就说会了"）、过关门限 `GATE=0.9`（`policy.py:35`，Alpha School "90% before you advance"）、复习间隔 `[0,1,3,7,14,30,60]` 天（`scheduler.py:14` 的 MEMORY 序列）。**状态是派生值**：只存作答记录，每次现算，不存"已掌握"字段。
- **老面板一个没删**，只整体搬进折叠 `<details class="plan-more">`，id 全部保留 → `studio.js` 的 refresh 照常命中。回滚 = 删 `studio.html` 两行。
- **`server.js` 新增 `GET /api/mastery` + `POST /api/mastery/attempt`**（手动 ✓/✗ 落 `data/mastery-attempts.json`，按考点分桶存最近 20 条）。
- **算法单测 36 项全绿**（`node` 跑真实 `学习日志.jsonl`）。过程中抓出并改掉**我自己编的规则**：复习档位原先写成"连续答对次数=档位"，改回逐条重放 `schedule_next` 状态机；日期格式化改本地时区（UTC+8 下 `toISOString` 整体差一天）。
- 🎯 **实测数据（09-13 00:18 首屏截图）**：有记录考点 **8 个 · 过关 0 个**；下一步自动指向「先清 Anki 到期：还剩 166 张」（到期 173 − 今日已做 7 = 166，两个数字对得上）。手动点 ✓ 已验证真能改数字：`C-for循环` 掌握度 0%→51%、做过 1→2 次（封顶 50%→80%），toast 正常、服务端落盘。

### 🚨 本轮最该记住的一条教训（与工具无关，是方法论）

**`focus.js` 写完后 `node --check` 通过、36 项单测全绿 —— 但它在浏览器里一次都没执行过。**
根因：`server.js` 的静态路由是**逐个登记白名单**，我只加了 `studio.html` 的 `<script>` 引用，忘了登记 `/focus.js`、`/focus.css`，结果 **404 静默失效**。连带查出 `desk.css`、`word.css` 早就在 404（页面只是"看着样式有点旧"，一直没人发现）。
→ **教训：语法检查和单测都证明不了"代码被加载了"。前端改动必须开浏览器看 console。** 今后新增根目录 css/js，`studio.html` 和 `server.js:ROOT_ASSETS` 两处都要改。
→ 同类风险：门户里任何"看起来在跑"的东西，先问一句"我亲眼在浏览器里见过它吗？"

### ✅ 家教（DeepTutor）从"点了没反应"到真能用

**推翻了长期结论「DeepTutor 冷启动很慢」** —— 是**假慢**。真因是启动器 `D:/studio/launch-deeptutor.cmd` 从来没把服务拉起来过，三层 bug 逐层剥：
1. 文件存成 **LF-only** → `cmd.exe` 错行解析，把中文注释切成 `launch`/`cal`/`1` 当命令跑，报"系统找不到指定的路径"却 **EXIT=0 假成功**（所以看起来"点了没反应"）；
2. 中文注释 + 无 `chcp` → cmd 按 GBK 解码，某些字节对吞掉行尾 CR 把下一行粘成碎命令；
3. **决定性一层**：`start "标题" /min cmd /c ...` 需要控制台，而门户是 `node spawn({stdio:'ignore'})` 无控制台调用 → 建不出窗口、子进程静默死亡。改用 `powershell Start-Process` 解决。
**实测**：点门户按钮 → **73 秒**就绪，3782/8001/9999 全 200，`/api/status` 里 `deeptutor online=true`。embedding 加载 bge 模型只用约 15 秒。
遗留（不影响可用）：启动器里 embedding 用裸 `python`（解析到系统 3.13.15 而非 `.venv`），日后若依赖 venv 包需换绝对路径。

### 📇 Anki 复测快照（09-13 00:18，门户直读 revlog）

今日到期 **173 张** · 今日已做 **7 张** · 逾期最久 **22 天** · 开课率 **44%**（已学 991 / 未学 1239）· 每日目标 50 张 → 还剩 43 张。
**与 09-12 17:30 那次对比：到期 180 → 173，说明中间只刷了个位数张。药方（每天 20-30 张清欠账、新词≤20）继续有效，未变。**

### 🖥️ 本机首个 C 编译器已装好（09-10 凌晨，用户拍板「本机装 w64devkit」）

**这条推翻了长期事实「本机无任何 C 编译器」** —— 今后出题可要求"写完自己跑一遍验证"，不再只能靠在线编辑器。

- **`skeeto/w64devkit` v2.9.1**（GitHub 4772★；实查 `get_latest_release` 确认 2026-08-10 发布、自带 **GCC 16.2**、61MB 免安装自解压。选型来自本轮 GitHub 实查，非凭记忆）
- **完整性双重校验通过**：下载 61462208 字节与官方一致；`certutil` SHA256 = `9208c19755cd4964b7915b9afcf02c66d493a4c870c4b3e83f6c538d9c1237a5`，与 GitHub asset `digest` **逐字符一致**
- 路径 `D:\w64devkit\w64devkit\bin\gcc.exe`（**套了两层 w64devkit**；PATH 必须给到 `bin`，否则 gcc 找不到同目录 `as.exe` → `CreateProcess: No such file or directory`）。占用 594M
- **一键入口 `D:\c-practice\run-c.bat`**：把 `.c` 文件**拖到图标上**即编译+运行；内容纯 ASCII（cmd 是 GBK，中文注释会乱码）。双路径已实跑验证：
  - ✅ 成功：`练习1.c` 输出 `A B A B C`（中文文件名正常）
  - ✅ 失败：`练习2-有错.c` 故意漏 2 个分号 → 精确报 `4:5: error: expected ',' or ';' before 'for'`，**带行号带波浪线**
- ✅ **SENDTO 已补上（09-10 凌晨，此条销账）**：`%APPDATA%\Microsoft\Windows\SENDTO\CC-compile-run.bat` 已写入并 `ls` 确认存在 → 现在**右键任意 `.c` 文件 → 发送到 → CC-compile-run** 即可编译+运行。桌面 .lnk 仍建不了（本机 PowerShell 是 ConstrainedLanguage 受限模式，`WScript.CreateObject` 被禁），拖文件到 `run-c.bat` 那条路照旧可用
- 🎯 **装它的教学理由**：考场没有在线编译器替他纠错。gcc 是"可控的报错环境"，用来练**读错误信息 + 逐字符自检**，不是用来依赖自动补全

### 讲义沉淀（应用户要求「整理文档沉淀下来」）

- 新建 **`docs/posts/computer/notes/1.3a-变量与赋值-零基础.md`**（263 行）—— 变量=盒子 / `=` 是从右往左"塞"不是相等 / for 的花括号管到哪一行 + `}` 是回程车票 / 完整人肉编译表格 / 自检 5 题+易错对照表+答案。直接对应本轮 ⑥ 和 B 题的两个错点
- 新建 **`docs/posts/math/notes/1.11-极限求值零基础-抓大头.md`**（179 行）—— **用 `$$` 真数学公式**（站点 `markdown.math:true` 已开）：lim 读法 / x→∞ 两种下场 / 抓大头 / 三条速查结论 / **⚠️ `x→∞` 与 `x→0` 分岔口（同一式子答案相反）** / 三步流程 / 自检 6 题+答案
- 已挂进侧边栏 `config.mts`（两处）+ 两个 `index.md`（含篇数 12→13）
- ✅ 死链检查 `node scripts/check-links.mjs`：**新增 4 条链接全部有效**；仅存的 7 处死链是历史遗留（政治 2025 的 `/resources/政治选择题题库` ×2、日语与 knowledge 的 `[[toc]]` ×5），与本次无关
- 错题本 `备考计划/错题本模板.md` 已填 **#C001**（B 题答 6 那题），`node scripts/mistake-stats.mjs` 实跑解析正常（错题 6→7）
- ✅ **讲义已上线（09-10 下午，此条销账）**：`npm run docs:build` 跑通（96.9s，上一轮的 2 条 `/resources/政治选择题题库` 死链已改成文字标注才过构建 —— 注意**死链会直接 fail build**，不是只警告）。实测门户 URL 全 200：
  - `http://127.0.0.1:43210/darling016123/posts/computer/notes/1.3a-变量与赋值-零基础.html`
  - `http://127.0.0.1:43210/darling016123/posts/math/notes/1.11-极限求值零基础-抓大头.html`
  - 📌 **门户前缀是 `/darling016123`（不是 `/notes`）**，`server.js` 的 `MOUNTS` 按 dist 的 base 挂载；构建完**不需重启门户**（静态文件直读 dist）
- 🆕 **应用户「主要是数学的公式」→ 建了离线自包含公式页 `D:\math-notes\高数极限-公式与人话对照.html`**（双击即开：不需联网、不需开门户、不需构建）：同目录带一份 `katex/`（与门户那份 md5 一致 `3ca9db95…`）；页面内引用已全部改为**相对路径**（原来的绝对 `file:///` 引用一旦挂到 http 就会被浏览器拒绝加载脚本，属真隐患，已修）。内容 = 樱花风 + **每条公式配一句人话** + 答案折叠 + ⑬a/⑬b/⑪ 实做。**验证：用页面自带引擎逐条 renderToString，46 处公式 0 失败**
- 📌 教训（写给教练自己）：**聊天窗口不渲染 LaTeX**，反斜杠源码对零基础等于没显示。今后聊天里出数学题一律用 **Unicode 人话排版**（`x→∞`、`3/x`、`√()`）；要真公式就指向这个离线页，别让用户啃 `\lim_`

### 历史（2026-09-07 凌晨～04:10）

- 知识库完善冲刺已推送（b8097c0）：新增 5 个必杀专项（3.4 循环数组 15 题 / 3.5 指针 / 3.6 递归 / 2.4a KMP / 2.5a 二叉树建树）、高数 2025 真题 OCR 文字化、1.10 极限三法决策树
- 桌面资料核查：`桌面/广东专升本资料/`（2.1GB）已基本文字化入 `资料/`（93 篇 md）；孤本排期待 11 月二轮（政治历年 doc、英语完形 100 篇、3400 词表、投档数据）
- "2000 必刷题"确认**未采购**，裁决：属第二轮（11 月）弹药，暂缓
- **Anki 闭环首跑完成（160e112）**：C01 基础语法 22 张 + 极限三法 12 张已真实入 Anki（牌组 专升本::C语言/专升本::高数，笔记类型"问答题"）；TSV 产物在 `knowledge/anki_cards/`
- **5 道试水题已作答并批改（≈25/100，真实基线）**：
  - 题1 for循环 0/20（只写出 `#include <stdio.h>`，自述"压根没写过"）｜题2 7/2 10/20（=3 ✅；得 3.5 的改法错——直觉摸到 float 方向）｜题3 (x²)′=2x 10/20（答案对但没写幂函数求导依据）｜题4 lim sinx/x 0/20（**不认识 lim 符号**——符号扫盲是头号缺口）｜题5 未作答
  - **6 张错题卡已入错题牌组**（专升本-C语言错题 3 张 / 专升本-高数错题 3 张，tag:试水题批改）——闭环第二环已实跑

## 📝 本周任务（2026-09-07 ~ 09-13）

- [x] ~~完成 5 道试水题并提交答案~~（09-07 已作答+批改+错题入库 ✅）
- [x] ~~交清欠的 3 道补测题~~（09-09 深夜已全交且全对：① =0 ✅ ② =3 ✅ ③ 判断+理由 ✅ → 高数 6 题 **100/100**）
- [ ] 🔴 **最高优先（新欠交，只剩两样）**：交 ③ C 题（`i<=3` 版 s 是几，**必须画 3 行表格**，答案 6）＋ ④ 花括号范围题（正确 **A B A B C** 共 5 行）
- [x] ~~B 题无脚手架重做~~（`a=a+2; a=a*2;` 09-09 答 6 → **09-10 凌晨合上讲义重做答 10 ✅**，同型新题答 14 ✅，#C001 已签，09-12 前再抽查一次防遗忘）
- [x] ~~lim 符号扫盲~~（09-09 晚间已过关：能读 lim/x→∞ 并翻译成人话 ✅）
- [x] ~~第三课·变量与赋值~~（09-10 凌晨已插队教完：变量=盒子 / `=` 是塞不是相等 / 花括号管到哪行 → 讲义已沉淀为 **1.3a**）
- [ ] 第三课续：**`scanf` 让程序"会听话"**；并把 `x→0` 分岔口实测掉（见待验证区）
- [ ] **本周一句话戒律**：不再改任何工具/脚本/门户。工具再不好用，也只记在「待办-工具」里，等本周日后再动（本轮已破戒，弹药到位即刻封刀）
- [ ] 开始清 Anki：40 张卡（C语言 22 + 高数 12 + 错题 6）首轮复习
- [ ] 建立 4-5h/天节奏（高数/C 交错，各 2h+）
- [ ] 本周只推 2 个考点：C01 基础语法、高数极限计算（配合新笔记 [1.10 极限三法决策树](/posts/math/notes/1.10-极限计算三法决策树)）
- [ ] 周末做交错小测 8 题 + 清一次 Anki 队列

## 🔍 待验证区（下次会话优先检查）

| 疑问 | 验证方式 |
|:---|:---|
| 🔴🔴 **09-12~09-13 整场是工具会话，学习量 = 0** | 这一轮只做门户/启动器/单测，**没做一道新题、没背一组词**（Anki 今日 7 张，远未到 50 张目标）。下面三条欠账**从 09-10 挂到现在一条没销**：#C002 `i<=3` 交 3 行表格（答案 6）、④ 花括号范围题（A B A B C）、$x\to0$ 分岔口实测。**下次会话禁止再开工具任务**，开场直接收这三样 —— 门户已建好、够用，不需要再改 |
| ✅ ~~假掌握复检：赋值覆盖~~ | **09-10 已过关**：合上讲义无表格独立做 → ① `a=a+2; a=a*2;` 答 **10** ✅；② `x=10; x=x-4; x=x+1; x=x*2;` 答 **14** ✅（10→6→7→14，三行连对）→ 同型连对 2 次，「`=` 从右往左 / 覆盖赋值」正式进进度条。错题本 #C001 复习记录已签。09-12 前再抽查一次防遗忘 |
| 🔴 **新发现的病：答案从上一题借** | ③ `for(i=1;i<=3;i++) s=s+i;` 答 **10**（正确 **6**），且**没交表格** —— 上一题 `i<=4` 恰好是 10，他直接搬过来了。**这不是算错是没算**。已登记 #C002。**新判据（今后严格执行）：只给数字不给过程 = 按错题重做**；下次必须交 3 行表格 |
| 🔴 **`x→0` 分岔口从未实测** | 只出一道：$\lim\limits_{x \to 0}\frac{1}{x}$ = ?。若答 0 → 说明把 $x\to\infty$ 的规则硬套过来了（同一式子箭头一换答案完全相反），回炉 [1.11 讲义第四节](/posts/math/notes/1.11-极限求值零基础-抓大头) |
| 🔴 **④ 花括号范围题未交** | 循环内两个 `printf`（A/B）+ 循环外一个（C），`i<=2` → 正确答案 **A B A B C**（5 行）。答对 = "括号内每轮全跑、括号外只跑一次"真的通了 |
| ✅ ~~会读了能不能算出来~~ | 09-09 深夜已验证：3 道欠交题全交全对（① =0 ② =3 ③ 自己写出理由）→ **「会读符号、不会算结果」缺口已闭合**，高数 6 题 **100/100**（同考点 09-09 白天是 50%） |
| ✅ ~~lim 符号是否真懂了~~ | 09-09 已验证：能读 lim/x→∞ 并翻译成人话（符号盲区已消除） |
| ✅ ~~第一个 C 程序能否独立写出~~ | 09-09 已验证：最小框架默写 20/20 全对。⚠️ 但**考场没有在线编译器**，已转入「人肉编译」训练，见上面两条 |
| Anki 40 张首轮 | 用户打开 Anki 实际刷，看板记录完成数（门户已新增 Anki 面板，revlog 数据不认自报） |
| 🔴 **FSRS 开关是否真的点了**（用户 09-10 表示自己去 Anki 开） | 开关位置、代价、风险见 **[Anki-FSRS开关-怎么开与代价](Anki-FSRS开关-怎么开与代价.md)**。**硬验证方式**：下次会话从 `collection.anki2` 副本查 `cards.data` 含 `"s":` 字段的卡数应 **> 0**（09-10 实查 **0/2271** = 仍在跑 SM-2）。不接受"我点了"作为凭据；同时确认期望保留率仍是默认 0.9 没被调高。**🔴 09-12 复验：仍是 0 / 2271 张含 `"s":` → FSRS 没开成，还在跑 SM-2。** 但**这不是当前的要紧事**：SM-2 排期本身没坏（保留率 94.3%），要修的是**手**（困难按太多）和**量**（新词开太快）。FSRS 继续挂账，等词库走上每天 20-30 张的节奏再开才有意义 |
| 每日 4-5h 是否兑现 | 用户自报 + 日志文件；连续 2 天未汇报要追问 |
| C01/C03/C04 概念掌握 | 交错小测（C 语言 5 题）≥70% 算过关，否则标记弱项 |
| 🔴 **课本调用机制是否真被使用**（09-10 建的，别让它变成死文档） | 下次会话**抽查教练自己**：任何 C 语言概念回答若没给「第4版第N页」引用 = 违约。用户可直接问"这句是查的还是背的"。脚本可复跑验证：`node scripts/textbook.mjs 自右而左` 应命中 p84 并打印 `[课本数据源]` |

## 📈 科目进度条

> 📐 **口径**：按**考点格子**算，不按感觉算。一格 = 该考点**撤掉讲义/表格后独立做对**。有脚手架做对不计入（见 09-10 的假掌握）。

| 科目 | 进度 | 详情 |
|:---|:---:|:---|
| C 语言 | ▃░░░░░░░░░ 8% | ✅ 已过 2 格：最小框架默写 20/20、**赋值覆盖（`=` 从右往左）连对 2 次**（09-10 深夜无表格重验）｜🔴 **for 循环仍未过**：`i<=3` 答 10（正确 6）→ 跳过计算搬上题答案；④ 花括号范围题未交。讲义 1.3a 已沉淀 |
| 数据结构 | ░░░░░░░░░░ 0% | 二轮任务；2.4a/2.5a 专项已备 |
| 高等数学 | ▃░░░░░░░░░ 8% | ✅ **极限求值 $x\to\infty$（含抓大头）已过关**：09-10 高数 6 题 100/100（09-09 白天同考点仅 50%）｜未测：$x\to0$ 分岔口、$\frac{0}{0}$ 型、导数依据 |
| 政治 | ░░░░░░░░░░ 0% | 三轮任务（12 月起） |
| 英语 | ▂░░░░░░░░░ 5% | Anki 2230 词牌组在刷（**保鲜轨道，不算主战场**） |

## 🧠 弱项画像（<70% 标记）

| 日期 | 知识点 | 得分 | 错误类型 |
|:---|:---|:---:|:---|
| 09-07 | C01 基础语法 | 25% | 程序框架残缺（无法独立写出 main）、整数除法细节（7.0/2 变换不会） |
| 09-07 | 高数-极限符号 | 0% | **符号盲区：不认识 lim/x→ 记号**（数学语言扫盲优先于解题技巧） |
| 09-07 | 高数-求导 | 50% | 会背 (x²)′=2x 但写不出依据（幂函数求导公式） |
| ~~09-09~~ | ~~高数-极限求值~~ | ~~50%~~ → **09-10 已 100%** | **应用错误型：会读符号，不会算结果** —— `lim(x→∞) 1/x` 只把条件翻译成人话就停（没写出"=0"）。✅ 09-07「符号盲区」已补，✅ 09-10 深夜「不会算结果」也已补（见下） |
| 09-09 | C 语言-程序最小框架 | **已掌握 20/20** | ✅ 默写全对（#include / int main() / 大括号 / return 0 / 分号），09-07 的框架残缺已消除 |
| ⚠️ 09-09 | 高数-术语书写 | — | 把「无限」写成「无线」；`printf("wpc ")` 引号内多一个空格 → 今后要求**逐字符自检输出**（考场没有编译器替他报错）。09-10 仍在写错，未改 |
| **09-10** | **高数-极限求值 + 抓大头** | **100%（100/100）** | ✅ 6 题全对：① =0 ② =3 ③ 判断+**自己写出理由**（"2x 无限趋近于无穷大怎么会等于 0"）④ =2 ⑤ =5。下一处风险在 $x\to 0$ 分岔口，**尚未实测** |
| **09-10** | **C 语言-for 循环 / 累计器 vs 计数器** | **0%（0/40）** | ❌ `for(i=1;i<=4;i++) s=s+i;` 答 **4**（正确 10）→ ①`i` 与 `s` 两个盒子混淆 ②以为循环只跑 1 轮 ③把 `i++` 当成"循环在干的事"。**根因在教练：变量/赋值没教就出循环题** |
| **09-10** | C 语言-赋值覆盖（`=` 从右往左） | **✅ 已掌握（连对 2 次）** | 先假掌握（有表格对、撤表格答 6）→ 当晚**合上讲义重做答 10 ✅**，同型新题 `x=10;x=x-4;x=x+1;x=x*2;` 答 **14 ✅**。#C001 已签；09-12 前抽查防遗忘 |
| **09-10** | 🔴 **C 语言-跳过计算 / 答案从上一题借** | **新发现，比前一条更危险** | `for(i=1;i<=3;i++) s=s+i;` 答 **10**（正确 **6**）且**没交表格**——上题 `i<=4` 恰好 10，直接搬答案。→ 立**硬判据：只给数字不给过程 = 按错题重做**，今后每次作业必须交表格。已登记 #C002 |
| 📌 09-10 | 教学纪律（**写给教练自己**） | — | 用户反馈「你是不是忘了我都是零基础」**成立**。五条硬规矩：①一次只教一个概念 ②没教过的不出题 ③他答错先自查是否我跳步 ④**判卷顺序：先看过程、后看数字**——只给数字没过程一律按错题重做（①② 数字对但没过程，仍只记半信，须同型复验）⑤**相邻两题刻意让答案不同**：本轮 ③ 正确答案 6、上题 10，他答 10 当场暴露"搬答案"——答案错开是**抓假会的探针**，不是替他防错 |

## 🗂️ 快速索引（教练开工即用的入口）

- 出题依据：`docs/posts/computer/notes/3.1~3.6 专项` + `历年真题/*/考点拆分/`
- 讲解依据：`资料/考点资源图谱-计算机.md`（视频推荐唯一依据，禁凭记忆推荐）
- 教材锚点：**C 语言 → 先查课本原文（见下条脚本）**；高数 B站宋浩、政治徐涛（这两科**无课本全文**，答案须标 ⚠️ 推算）
- 🆕 **📕 课本原文优先机制已落地（09-10，用户要求「遇到知识问题调用原课本来思考」）**：
  - **调用入口**：`node scripts/textbook.mjs <概念词>` / `--page N` / `--pages A-B` / `--toc` / `--ch N`（仓库内脚本，只读，秒级返回）
  - 输出自带**可信度标注**（`✅ 散文` / `⚠️ 含代码` / `⚠️ 含公式(可能乱码)`），并在末尾打印 `[课本数据源]` 路径作为**取证凭证**（防止凭记忆假装查过）
  - 数据源两份副本自动查找：仓库 `guangdong-zhuanshengben-resources\计算机基础与程序设计\《C语言程序设计》.txt`（**已确认被 .gitignore 拦住，不进 git**）→ 桌面原件 → 环境变量 `TEXTBOOK_TXT`
  - **硬协议已写进 `skill:study-coach`**（真实加载路径 `AppData\Roaming\reasonix\global-workspace\.reasonix\skills\study-coach\SKILL.md`）：讲概念/判卷/记忆冲突/无命中四个时机**必须查**，引用一律给「第4版第N页」，禁止编造页码
  - 已实测锚点：**p81**「分号是语句中不可缺少的组成部分，而不是两个语句间的分隔符号」；**p84**「赋值运算符按自右而左结合」（正是 #C001 出处）
  - 三条红线不变：①禁止复制其中代码（右花括号少 142 个）②含公式题干须回原 PDF 核对（4.3 已被抽取毁）③有版权不进 git，只登记页码指针。**用法与硬伤必读 [[谭浩强C语言第4版-txt体检与用法]]**
- Anki 排期算法开关说明：[[Anki-FSRS开关-怎么开与代价]]（SM-2 vs FSRS、代价与风险、验证方式；**AI 不代改 Anki 设置**）
- 学习事件流水账：`备考计划/学习日志.jsonl` → 门户「🪞 复盘」页读它（`GET /api/log`）
- 备考时间线：[[2027届-完整备考时间线]] · 自测表：[[进度自测表]]

---

> 🤖 **给下一个 AI 会话的交接指令**：读完本文件后，1) 以「上次会话完成的事」和「本周任务」为上下文开场；2) 优先处理「待验证区」；3) 会话中的学习事件（做题/批改/新弱项）在结束前写回本文件并更新 `last_updated`。
