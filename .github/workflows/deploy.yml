name: Deploy VitePress site to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Diagnostics
        run: |
          node --version
          npm --version
          git log --oneline -3
          echo "--- npm config ---"
          npm config get registry

      - name: Install
        run: npm ci

      - name: Build
        env:
          GITHUB_ACTIONS: true
        run: npm run docs:build

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v5
        with:
          path: docs/.vitepress/dist

      # 跨项目联动：构建成功后通知 ai-learning-assistant 重建 RAG 知识库
      # 前提：仓库配置 PAT（Actions secrets → RAG_TRIGGER_TOKEN，需 repo 权限）
      # 未配置时该步骤静默跳过，ai-learning-assistant 每天凌晨 4:00 仍有兜底重建
      - name: 🔄 通知 ai-learning-assistant 重建 RAG
        if: success() && env.RAG_TRIGGER_TOKEN != ''
        env:
          RAG_TRIGGER_TOKEN: ${{ secrets.RAG_TRIGGER_TOKEN }}
        run: |
          curl -s -X POST \
            -H "Accept: application/vnd.github+json" \
            -H "Authorization: Bearer $RAG_TRIGGER_TOKEN" \
            -H "X-GitHub-Api-Version: 2022-11-28" \
            https://api.github.com/repos/wpc725562-dotcom/ai-learning-assistant/dispatches \
            -d '{"event_type":"rag-rebuild"}' \
            && echo "✅ 已通知 ai-learning-assistant 重建 RAG" \
            || echo "ℹ️ 通知失败（忽略，依赖 ai-learning-assistant 每日兜底）"

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v5
